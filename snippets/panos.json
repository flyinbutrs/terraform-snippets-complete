{
    "panos-datasource-dhcp-interface-info": {
        "body": "data \"panos_dhcp_interface_info\" \"$1\" {\n    # Exported Attributes\n}",
        "description": "Use this data source to retrieve DHCP client information about the given\nfirewall interface.",
        "prefix": "panos-datasource-dhcp-interface-info"
    },
    "panos-datasource-system-info": {
        "body": "data \"panos_system_info\" \"$1\" {\n    # Exported Attributes\n}",
        "description": "Use this data source to retrieve \"show system info\" from the NGFW or Panorama. All contents of \"show system info\" are saved to the `info` variable.  In\naddition, the version number of PAN-OS encountered is saved to multiple\nfields for ease of access.",
        "prefix": "panos-datasource-system-info"
    },
    "panos-panorama-resource-address-group": {
        "body": "resource \"panos_panorama_address_group\" \"$1\" {\n    # name - (Required) The address group's name.\n    name = \"\"\n\n    # device_group - (Optional) The device group to put the address group into\n    device_group = \"\"\n\n    # static_addresses - (Optional) The address objects to include in this\n    static_addresses = \"\"\n\n    # dynamic_match - (Optional) The IP tags to include in this DAG.\n    dynamic_match = \"\"\n\n    # description - (Optional) The address group's description.\n    description = \"\"\n\n    # tags - (Optional) List of administrative tags.\n    tags = \"\"\n\n    # Exported Attributes\n}",
        "description": "This resource allows you to add/update/delete Panorama address groups. Address groups are either statically defined or dynamically defined, so only\n`static_addresses` or `dynamic_match` should be defined within a given address\ngroup.",
        "prefix": "panos-panorama-resource-address-group"
    },
    "panos-panorama-resource-address-object": {
        "body": "resource \"panos_panorama_address_object\" \"$1\" {\n    # value - (Required) The address object's value.  This can take various\n    value = \"\"\n\n    # name - (Required) The address object's name.\n    name = \"\"\n\n    # device_group - (Optional) The device group to put the address object\n    device_group = \"\"\n\n    # type - (Optional) The type of address object.  This can be `ip-netmask`\n    type = \"\"\n\n    # description - (Optional) The address object's description.\n    description = \"\"\n\n    # tags - (Optional) List of administrative tags.\n    tags = \"\"\n\n    # Exported Attributes\n}",
        "description": "This resource allows you to add/update/delete address objects on Panorama.",
        "prefix": "panos-panorama-resource-address-object"
    },
    "panos-panorama-resource-administrative-tag": {
        "body": "resource \"panos_panorama_administrative_tag\" \"$1\" {\n    # name - (Required) The administrative tag's name.\n    name = \"\"\n\n    # device_group - (Optional) The device group to put the administrative tag into\n    device_group = \"\"\n\n    # color - (Optional) The tag's color.  This should be either an empty string\n    color = \"\"\n\n    # comment - (Optional) The administrative tag's description.\n    comment = \"\"\n\n    # Exported Attributes\n}",
        "description": "This resource allows you to add/update/delete Panorama administrative tags.",
        "prefix": "panos-panorama-resource-administrative-tag"
    },
    "panos-panorama-resource-device-group": {
        "body": "resource \"panos_panorama_device_group\" \"$1\" {\n    # serial - (Required) The serial number of the firewall.\n    serial = \"\"\n\n    # name - (Required) The device group's name.\n    name = \"\"\n\n    # description - (Optional) The device group's description.\n    description = \"\"\n\n    # device - (Optional) The device definition (see below).\n    device = \"\"\n\n    # vsys_list - (Optional) A subset of all available vsys on the firewall\n    vsys_list = \"\"\n\n    # Exported Attributes\n}",
        "description": "This resource allows you to add/update/delete Panorama device groups. This resource has some overlap with the `panos_panorama_device_group_entry`\nresource.  If you want to use this resource with the other one, then make\nsure that your `panos_panorama_device_group` spec does not define any\n`device` blocks, and just stays as \"computed\". This is the appropriate resource to use if `terraform destroy` should delete\nthe device group.",
        "prefix": "panos-panorama-resource-device-group"
    },
    "panos-panorama-resource-device-group-entry": {
        "body": "resource \"panos_panorama_device_group_entry\" \"$1\" {\n    # serial - (Required) The serial number of the firewall.\n    serial = \"\"\n\n    # device_group - (Required) The device group's name.\n    device_group = \"\"\n\n    # vsys_list - (Optional) A subset of all available vsys on the firewall\n    vsys_list = \"\"\n\n    # Exported Attributes\n}",
        "description": "This resource allows you to add/update/delete a specific device in a Panorama\ndevice group. This resource has some overlap with the `panos_panorama_device_group`\nresource.  If you want to use this resource with the other one, then make\nsure that your `panos_panorama_device_group` spec does not define any\n`device` blocks, and just stays as \"computed\". This is the appropriate resource to use if you have a pre-existing device group\nin Panorama and don't want Terraform to delete it on `terraform destroy`. An interesting side effect of the underlying XML API - if the device group does\nnot already exist, then this resource can actually create it.  However, since\nonly the single entry for the specific serial number is deleted, then a\n`terraform destroy` would not remove the device group itself in this situation.",
        "prefix": "panos-panorama-resource-device-group-entry"
    },
    "panos-panorama-resource-ethernet-interface": {
        "body": "resource \"panos_panorama_ethernet_interface\" \"$1\" {\n    # mode - (Required) The interface mode.  This can be any of the following\n    mode = \"\"\n\n    # template - (Required) The template name.\n    template = \"\"\n\n    # name - (Required) The ethernet interface's name.  This should be something\n    name = \"\"\n\n    # vsys - (Optional) The vsys that will use this interface (default:\n    vsys = \"\"\n\n    # static_ips - (Optional) List of static IPv4 addresses to set for this data\n    static_ips = \"\"\n\n    # enable_dhcp - (Optional) Set to `true` to enable DHCP on this interface.\n    enable_dhcp = \"\"\n\n    # create_dhcp_default_route - (Optional) Set to `true` to create a DHCP\n    create_dhcp_default_route = \"\"\n\n    # dhcp_default_route_metric - (Optional) The metric for the DHCP default\n    dhcp_default_route_metric = \"\"\n\n    # management_profile - (Optional) The management profile.\n    management_profile = \"\"\n\n    # mtu - (Optional) The MTU.\n    mtu = \"\"\n\n    # adjust_tcp_mss - (Optional) Adjust TCP MSS (default: false).\n    adjust_tcp_mss = \"\"\n\n    # netflow_profile - (Optional) The netflow profile.\n    netflow_profile = \"\"\n\n    # lldp_enabled - (Optional) Enable LLDP (default: false).\n    lldp_enabled = \"\"\n\n    # lldp_profile - (Optional) LLDP profile.\n    lldp_profile = \"\"\n\n    # link_speed - (Optional) Link speed.  This can be any of the following:\n    link_speed = \"\"\n\n    # link_duplex - (Optional) Link duplex setting.  This can be `full`, `half`,\n    link_duplex = \"\"\n\n    # link_state - (Optional) The link state.  This can be `up`, `down`, or\n    link_state = \"\"\n\n    # auto - (Optional) `auto`.\n    auto = \"\"\n\n    # aggregate_group - (Optional) The aggregate group (applicable for\n    aggregate_group = \"\"\n\n    # comment - (Optional) The interface comment.\n    comment = \"\"\n\n    # Exported Attributes\n}",
        "description": "This resource allows you to add/update/delete Panorama ethernet interfaces\nfor templates.",
        "prefix": "panos-panorama-resource-ethernet-interface"
    },
    "panos-panorama-resource-ike-crypto-profile": {
        "body": "resource \"panos_panorama_ike_crypto_profile\" \"$1\" {\n    # name - (Required) The object's name\n    name = \"\"\n\n    # template - (Optional) The template name.\n    template = \"\"\n\n    # template_stack - (Optional) The template stack name.\n    template_stack = \"\"\n\n    # dh_groups - (Optional) (Required, list) List of DH Group entries.  Values should\n    dh_groups = \"\"\n\n    # authentications - (Optional) (Required, list) List of authentication types.  This c\n    authentications = \"\"\n\n    # encryptions - (Optional) (Required, list) List of encryption types.  Valid values\n    encryptions = \"\"\n\n    # lifetime_type - (Optional) The lifetime type.  Valid values are `seconds`,\n    lifetime_type = \"\"\n\n    # minutes - (Optional) `minutes`, `hours` (the default), and `days`.\n    minutes = \"\"\n\n    # lifetime_value - (Optional) (Optional, int) The lifetime value.\n    lifetime_value = \"\"\n\n    # authentication_multiple - (Optional) (Optional, PAN-OS 7.0+, int) IKEv2 SA\n    authentication_multiple = \"\"\n\n    # Exported Attributes\n}",
        "description": "This resource allows you to add/update/delete Panorama IKE crypto profiles\nto a template or template stack.",
        "prefix": "panos-panorama-resource-ike-crypto-profile"
    },
    "panos-panorama-resource-ike-gateway": {
        "body": "resource \"panos_panorama_ike_gateway\" \"$1\" {\n    # interface - (Required) The interface.\n    interface = \"\"\n\n    # name - (Required) The object's name\n    name = \"\"\n\n    # template - (Optional) The template name.\n    template = \"\"\n\n    # template_stack - (Optional) The template stack name.\n    template_stack = \"\"\n\n    # version - (Optional) (Optional, PAN-OS 7.0+) The IKE gateway version.  Valid values are\n    version = \"\"\n\n    # disabled - (Optional) (Optional, PAN-OS 7.0+, bool) Set to `true` to disable.\n    disabled = \"\"\n\n    # peer_ip_type - (Optional) The peer IP type.  Valid values are `ip`,\n    peer_ip_type = \"\"\n\n    # dynamic - (Optional) `dynamic`, and `fqdn` (PANOS 8.1+).\n    dynamic = \"\"\n\n    # peer_ip_value - (Optional) The peer IP value.\n    peer_ip_value = \"\"\n\n    # local_ip_address_type - (Optional) The local IP address type.  Valid\n    local_ip_address_type = \"\"\n\n    # local_ip_address_value - (Optional) The IP address if `local_ip_address_type`\n    local_ip_address_value = \"\"\n\n    # auth_type - (Optional) The auth type.  Valid values are `pre-shared-key`\n    auth_type = \"\"\n\n    # pre_shared_key - (Optional) The pre-shared key value.\n    pre_shared_key = \"\"\n\n    # local_id_type - (Optional) The local ID type.  Valid values are `ipaddr`,\n    local_id_type = \"\"\n\n    # fqdn - (Optional) `fqdn`, `ufqdn`, `keyid`, or `dn`.\n    fqdn = \"\"\n\n    # local_id_value - (Optional) The local ID value.\n    local_id_value = \"\"\n\n    # peer_id_type - (Optional) The peer ID type.  Valid values are `ipaddr`,\n    peer_id_type = \"\"\n\n    # fqdn - (Optional) `fqdn`, `ufqdn`, `keyid`, or `dn`.\n    fqdn = \"\"\n\n    # peer_id_value - (Optional) The peer ID value.\n    peer_id_value = \"\"\n\n    # peer_id_check - (Optional) Enable peer ID wildcard match for certificate\n    peer_id_check = \"\"\n\n    # local_cert - (Optional) The local certificate name.\n    local_cert = \"\"\n\n    # cert_enable_hash_and_url - (Optional) (Optional, PAN-OS 7.0+, bool) Set to `true` to use\n    cert_enable_hash_and_url = \"\"\n\n    # cert_base_url - (Optional) The host and directory part of URL for local\n    cert_base_url = \"\"\n\n    # cert_use_management_as_source - (Optional) (Optional, PAN-OS 7.0+, bool) Set to `true` to\n    cert_use_management_as_source = \"\"\n\n    # cert_permit_payload_mismatch - (Optional) (Optional, bool) Set to `true` to permit\n    cert_permit_payload_mismatch = \"\"\n\n    # cert_profile - (Optional) Profile for certificate valdiation during IKE\n    cert_profile = \"\"\n\n    # cert_enable_strict_validation - (Optional) (Optional, bool) Set to `true` to enable\n    cert_enable_strict_validation = \"\"\n\n    # enable_passive_mode - (Optional) (Optional, bool) Set to `true` to enable passive\n    enable_passive_mode = \"\"\n\n    # enable_nat_traversal - (Optional) (Optional, bool) Set to `true` to enable NAT\n    enable_nat_traversal = \"\"\n\n    # nat_traversal_keep_alive - (Optional) (Optional, int) Sending interval for NAT\n    nat_traversal_keep_alive = \"\"\n\n    # nat_traversal_enable_udp_checksum - (Optional) (Optional, bool) Set to `true` to enable\n    nat_traversal_enable_udp_checksum = \"\"\n\n    # enable_fragmentation - (Optional) (Optional, bool) Set to `true` to enable fragmentation.\n    enable_fragmentation = \"\"\n\n    # enable_dead_peer_detection - (Optional) (Optional, bool) Set to `true` to enable dead\n    enable_dead_peer_detection = \"\"\n\n    # dead_peer_detection_interval - (Optional) (Optional, int) The dead peer detection interval.\n    dead_peer_detection_interval = \"\"\n\n    # dead_peer_detection_retry - (Optional) (Optional, int) Number of retries before disconnection.\n    dead_peer_detection_retry = \"\"\n\n    # enable_liveness_check - (Optional) (Optional, , PAN-OS 7.0+bool) Set to `true` to\n    enable_liveness_check = \"\"\n\n    # liveness_check_interval - (Optional) (Optional, , PAN-OS 7.0+int) Delay interval before\n    liveness_check_interval = \"\"\n\n    # Exported Attributes\n}",
        "description": "This resource allows you to add/update/delete Panorama IKE gateways\nfor both templates and template stacks.",
        "prefix": "panos-panorama-resource-ike-gateway"
    },
    "panos-panorama-resource-ipsec-crypto-profile": {
        "body": "resource \"panos_panorama_ipsec_crypto_profile\" \"$1\" {\n    # name - (Required) The object's name\n    name = \"\"\n\n    # template - (Optional) The template name.\n    template = \"\"\n\n    # template_stack - (Optional) The template stack name.\n    template_stack = \"\"\n\n    # protocol - (Optional) The protocol.  Valid values are `esp` (the default)\n    protocol = \"\"\n\n    # authentications - (Optional) List of authentication types.\n    authentications = \"\"\n\n    # encryptions - (Optional) List of encryption types.  Valid values\n    encryptions = \"\"\n\n    # dh_group - (Optional) The DH group value.  Valid values should start with\n    dh_group = \"\"\n\n    # lifetime_type - (Optional) The lifetime type.  Valid values are `seconds`,\n    lifetime_type = \"\"\n\n    # minutes - (Optional) `minutes`, `hours` (the default), or `days`.\n    minutes = \"\"\n\n    # lifetime_value - (Optional) (Optional, int) The lifetime value.\n    lifetime_value = \"\"\n\n    # lifesize_type - (Optional) The lifesize type.  Valid values are `kb`, `mb`,\n    lifesize_type = \"\"\n\n    # gb - (Optional) `gb`, or `tb`.\n    gb = \"\"\n\n    # lifesize_value - (Optional) (Optional, int) the lifesize value.\n    lifesize_value = \"\"\n\n    # Exported Attributes\n}",
        "description": "This resource allows you to add/update/delete Panorama IPSec crypto profiles\nfor both templates and template stacks.",
        "prefix": "panos-panorama-resource-ipsec-crypto-profile"
    },
    "panos-panorama-resource-ipsec-tunnel": {
        "body": "resource \"panos_panorama_ipsec_tunnel\" \"$1\" {\n    # tunnel_interface - (Required) The tunnel interface.\n    tunnel_interface = \"\"\n\n    # template - (Required) The template name.\n    template = \"\"\n\n    # name - (Required) The object's name\n    name = \"\"\n\n    # anti_replay - (Optional) (Optional, bool) Set to `true` to enable Anti-Replay check\n    anti_replay = \"\"\n\n    # copy_tos - (Optional) (Optional, bool) Set to `true` to copy IP TOS bits from inner\n    copy_tos = \"\"\n\n    # copy_flow_label - (Optional) (Optional, PAN-OS 7.0+, bool) Set to `true` to copy IPv6\n    copy_flow_label = \"\"\n\n    # disabled - (Optional) (Optional, PAN-OS 7.0+, bool) Set to `true` to disable this\n    disabled = \"\"\n\n    # type - (Optional) The type.  Valid values are `auto-key` (the default),\n    type = \"\"\n\n    # ak_ike_gateway - (Optional) IKE gateway name.\n    ak_ike_gateway = \"\"\n\n    # ak_ipsec_crypto_profile - (Optional) IPSec crypto profile name.\n    ak_ipsec_crypto_profile = \"\"\n\n    # mk_local_spi - (Optional) Outbound SPI, hex format.\n    mk_local_spi = \"\"\n\n    # mk_remote_spi - (Optional) Inbound SPI, hex format.\n    mk_remote_spi = \"\"\n\n    # mk_local_address_ip - (Optional) Specify exact IP address if interface\n    mk_local_address_ip = \"\"\n\n    # mk_local_address_floating_ip - (Optional) Floating IP address in HA\n    mk_local_address_floating_ip = \"\"\n\n    # mk_protocol - (Optional) Manual key protocol.  Valid valies are\n    mk_protocol = \"\"\n\n    # esp - (Optional) `esp` or `ah`.\n    esp = \"\"\n\n    # mk_auth_type - (Optional) Authentication algorithm.  Valid values are\n    mk_auth_type = \"\"\n\n    # mk_auth_key - (Optional) The auth key for the given auth type.\n    mk_auth_key = \"\"\n\n    # mk_esp_encryption_type - (Optional) The encryption algorithm.  Valid values\n    mk_esp_encryption_type = \"\"\n\n    # mk_esp_encryption_key - (Optional) The encryption key.\n    mk_esp_encryption_key = \"\"\n\n    # gps_interface - (Optional) Interface to communicate with portal.\n    gps_interface = \"\"\n\n    # gps_portal_address - (Optional) GlobalProtect portal address.\n    gps_portal_address = \"\"\n\n    # gps_publish_connected_routes - (Optional) (Optional, bool) Set to `true` to to publish\n    gps_publish_connected_routes = \"\"\n\n    # gps_publish_routes - (Optional) (Optional, list) Specify list of routes to publish\n    gps_publish_routes = \"\"\n\n    # gps_local_certificate - (Optional) GlobalProtect satellite certificate\n    gps_local_certificate = \"\"\n\n    # gps_certificate_profile - (Optional) Profile for authenticating\n    gps_certificate_profile = \"\"\n\n    # enable_tunnel_monitor - (Optional) (Optional, bool) Enable tunnel monitoring on this tunnel.\n    enable_tunnel_monitor = \"\"\n\n    # tunnel_monitor_destination_ip - (Optional) Destination IP to send ICMP probe.\n    tunnel_monitor_destination_ip = \"\"\n\n    # tunnel_monitor_source_ip - (Optional) Source IP to send ICMP probe\n    tunnel_monitor_source_ip = \"\"\n\n    # tunnel_monitor_profile - (Optional) Tunnel monitor profile.\n    tunnel_monitor_profile = \"\"\n\n    # tunnel_monitor_proxy_id - (Optional) (Optional, PAN-OS 7.0+) Which proxy-id (or\n    tunnel_monitor_proxy_id = \"\"\n\n    # Exported Attributes\n}",
        "description": "This resource allows you to add/update/delete Panorama IPSec tunnels\nfor templates. A large number of params have prefixes: `ak` - Auto key  `mk` - Manual key  `gps` - GlobalProtect Satellite ",
        "prefix": "panos-panorama-resource-ipsec-tunnel"
    },
    "panos-panorama-resource-ipsec-tunnel-proxy-id-ipv4": {
        "body": "resource \"panos_panorama_ipsec_tunnel_proxy_id_ipv4\" \"$1\" {\n    # ipsec_tunnel - (Required) The auto key IPSec tunnel to attach this \n    ipsec_tunnel = \"\"\n\n    # name - (Required) The object's name\n    name = \"\"\n\n    # template - (Required) The template name.\n    template = \"\"\n\n    # local - (Optional) IP subnet or IP address represents local network.\n    local = \"\"\n\n    # remote - (Optional) IP subnet or IP address represents remote network.\n    remote = \"\"\n\n    # protocol_any - (Optional) (Optional, bool) Set to `true` for any IP protocol.\n    protocol_any = \"\"\n\n    # protocol_number - (Optional) (Optional, int) IP protocol number.\n    protocol_number = \"\"\n\n    # protocol_tcp_local - (Optional) (Optional, int) Local TCP port number.\n    protocol_tcp_local = \"\"\n\n    # protocol_tcp_remote - (Optional) (Optional, int) Remote TCP port number.\n    protocol_tcp_remote = \"\"\n\n    # protocol_udp_local - (Optional) (Optional, int) Local UDP port number.\n    protocol_udp_local = \"\"\n\n    # protocol_udp_remote - (Optional) (Optional, int) Remote UDP port number.\n    protocol_udp_remote = \"\"\n\n    # Exported Attributes\n}",
        "description": "This resource allows you to add/update/delete Panorama IPSec tunnel proxy IDs\nto a parent auto key IPSec tunnel for templates.",
        "prefix": "panos-panorama-resource-ipsec-tunnel-proxy-id-ipv4"
    },
    "panos-panorama-resource-loopback-interface": {
        "body": "resource \"panos_panorama_loopback_interface\" \"$1\" {\n    # template - (Required) The template name.\n    template = \"\"\n\n    # name - (Required) The interface's name.  This must start with `loopback.`.\n    name = \"\"\n\n    # vsys - (Optional) The vsys that will use this interface (default: `vsys1`).\n    vsys = \"\"\n\n    # comment - (Optional) The interface comment.\n    comment = \"\"\n\n    # netflow_profile - (Optional) The netflow profile.\n    netflow_profile = \"\"\n\n    # static_ips - (Optional) List of static IPv4 addresses to set for this data\n    static_ips = \"\"\n\n    # management_profile - (Optional) The management profile.\n    management_profile = \"\"\n\n    # mtu - (Optional) The MTU.\n    mtu = \"\"\n\n    # adjust_tcp_mss - (Optional) (Optional, bool) Adjust TCP MSS (default: false).\n    adjust_tcp_mss = \"\"\n\n    # Exported Attributes\n}",
        "description": "This resource allows you to add/update/delete Panorama loopback interfaces\nfor templates.",
        "prefix": "panos-panorama-resource-loopback-interface"
    },
    "panos-panorama-resource-management-profile": {
        "body": "resource \"panos_panorama_management_profile\" \"$1\" {\n    # name - (Required) The management profile's name.\n    name = \"\"\n\n    # template - (Optional) The template name.\n    template = \"\"\n\n    # template_stack - (Optional) The template stack name.\n    template_stack = \"\"\n\n    # ping - (Optional) Allow ping.\n    ping = \"\"\n\n    # telnet - (Optional) Allow telnet.\n    telnet = \"\"\n\n    # ssh - (Optional) Allow SSH.\n    ssh = \"\"\n\n    # http - (Optional) Allow HTTP.\n    http = \"\"\n\n    # http_ocsp - (Optional) Allow HTTP OCSP.\n    http_ocsp = \"\"\n\n    # https - (Optional) Allow HTTPS.\n    https = \"\"\n\n    # snmp - (Optional) Allow SNMP.\n    snmp = \"\"\n\n    # response_pages - (Optional) Allow response pages.\n    response_pages = \"\"\n\n    # userid_service - (Optional) Allow User ID service.\n    userid_service = \"\"\n\n    # userid_syslog_listener_ssl - (Optional) Allow User ID syslog listener\n    userid_syslog_listener_ssl = \"\"\n\n    # userid_syslog_listener_udp - (Optional) Allow User ID syslog listener\n    userid_syslog_listener_udp = \"\"\n\n    # permitted_ips - (Optional) The list of permitted IP addresses or address\n    permitted_ips = \"\"\n\n    # Exported Attributes\n}",
        "description": "This resource allows you to add/update/delete Panorama interface management profiles\nfor both templates and template stacks.",
        "prefix": "panos-panorama-resource-management-profile"
    },
    "panos-panorama-resource-nat-rule": {
        "body": "resource \"panos_panorama_nat_rule\" \"$1\" {\n    # serial - (Required) The serial number of the firewall.\n    serial = \"\"\n\n    # destination_addresses - (Required) List of destination address(es).\n    destination_addresses = \"\"\n\n    # source_addresses - (Required) List of source address(es).\n    source_addresses = \"\"\n\n    # destination_zone - (Required) The destination zone.\n    destination_zone = \"\"\n\n    # source_zones - (Required) The list of source zone(s).\n    source_zones = \"\"\n\n    # name - (Required) The NAT rule's name.\n    name = \"\"\n\n    # device_group - (Optional) The device group to put the NAT rule into\n    device_group = \"\"\n\n    # rulebase - (Optional) The rulebase.  This can be `pre-rulebase` (default),\n    rulebase = \"\"\n\n    # description - (Optional) The description.\n    description = \"\"\n\n    # type - (Optional) \n    type = \"\"\n\n    # to_interface - (Optional) Egress interface from route lookup (default:\n    to_interface = \"\"\n\n    # any - (Optional) `any`).\n    any = \"\"\n\n    # service - (Optional) Service (default: `any`).\n    service = \"\"\n\n    # sat_type - (Optional) Type of source address translation.  This can be\n    sat_type = \"\"\n\n    # none - (Optional) `none` (default), `dynamic-ip-and-port`, `dynamic-ip`, or `static-ip`.\n    none = \"\"\n\n    # sat_address_type - (Optional) Source address translation address type.\n    sat_address_type = \"\"\n\n    # sat_translated_addresses - (Optional) Source address translation list of\n    sat_translated_addresses = \"\"\n\n    # sat_interface - (Optional) Source address translation interface.\n    sat_interface = \"\"\n\n    # sat_ip_address - (Optional) Source address translation IP address.\n    sat_ip_address = \"\"\n\n    # sat_fallback_type - (Optional) Source address translation fallback type.\n    sat_fallback_type = \"\"\n\n    # sat_fallback_translated_addresses - (Optional) Source address translation\n    sat_fallback_translated_addresses = \"\"\n\n    # sat_fallback_interface - (Optional) Source address translation fallback\n    sat_fallback_interface = \"\"\n\n    # sat_fallback_ip_type - (Optional) Source address translation fallback\n    sat_fallback_ip_type = \"\"\n\n    # sat_fallback_ip_address - (Optional) The source address translation\n    sat_fallback_ip_address = \"\"\n\n    # sat_static_translated_address - (Optional) The statically translated source\n    sat_static_translated_address = \"\"\n\n    # sat_static_bi_directional - (Optional) Set to `true` to enable\n    sat_static_bi_directional = \"\"\n\n    # dat_type - (Optional) Destination address translation type.  This should\n    dat_type = \"\"\n\n    # dat_address - (Optional) Destination address translation's address.  Requires\n    dat_address = \"\"\n\n    # dat_type - (Optional) `dat_type` be set to \"static\" or \"dynamic\".\n    dat_type = \"\"\n\n    # dat_port - (Optional) Destination address translation's port number.  Requires\n    dat_port = \"\"\n\n    # dat_type - (Optional) `dat_type` be set to \"static\" or \"dynamic\".\n    dat_type = \"\"\n\n    # dat_dynamic_distribution - (Optional) (Optional, PAN-OS 8.1+) Distribution algorithm\n    dat_dynamic_distribution = \"\"\n\n    # disabled - (Optional) Set to `true` to disable this rule.\n    disabled = \"\"\n\n    # tags - (Optional) List of administrative tags.\n    tags = \"\"\n\n    # target - (Optional) A target definition (see below).  If there are no\n    target = \"\"\n\n    # negate_target - (Optional) (Optional, bool) Instead of applying the rule for the\n    negate_target = \"\"\n\n    # vsys_list - (Optional) A subset of all available vsys on the firewall\n    vsys_list = \"\"\n\n    # Exported Attributes\n}",
        "description": "This resource allows you to add/update/delete Panorama NAT rules. ~> **Note:** `panos_panorama_nat_policy` is known as `panos_panorama_nat_rule`. The prefix `sat` stands for \"Source Address Translation\" while the prefix \"dat\"\nstands for \"Destination Address Translation\".  The order of the params in\nthis resource and their naming matches how the params are presented in\nthe GUI.  Thus, having a GUI window open while creating your resource\ndefinition will simplify the process. Note that while many of the params for this resource are optional in an\nabsolute sense, depending on what type of NAT you wish to configure, certain\nparams may become necessary to correctly configure the NAT rule.",
        "prefix": "panos-panorama-resource-nat-rule"
    },
    "panos-panorama-resource-security-policy": {
        "body": "resource \"panos_panorama_security_policy\" \"$1\" {\n    # serial - (Required) The serial number of the firewall.\n    serial = \"\"\n\n    # categories - (Required) List of categories.\n    categories = \"\"\n\n    # services - (Required) List of services.\n    services = \"\"\n\n    # applications - (Required) List of applications.\n    applications = \"\"\n\n    # destination_addresses - (Required) List of destination addresses.\n    destination_addresses = \"\"\n\n    # destination_zones - (Required) List of destination zones.\n    destination_zones = \"\"\n\n    # hip_profiles - (Required) List of HIP profiles.\n    hip_profiles = \"\"\n\n    # source_users - (Required) List of source users.\n    source_users = \"\"\n\n    # source_addresses - (Required) List of source addresses.\n    source_addresses = \"\"\n\n    # source_zones - (Required) List of source zones.\n    source_zones = \"\"\n\n    # name - (Required) The security rule name.\n    name = \"\"\n\n    # device_group - (Optional) The device group to put the security policy into\n    device_group = \"\"\n\n    # rulebase - (Optional) The rulebase.  This can be `pre-rulebase` (default),\n    rulebase = \"\"\n\n    # rule - (Optional) The security rule definition (see below).  The security rule\n    rule = \"\"\n\n    # type - (Optional) Rule type.  This can be `universal` (default),\n    type = \"\"\n\n    # interzone - (Optional) `interzone`, or `intrazone`.\n    interzone = \"\"\n\n    # description - (Optional) The description.\n    description = \"\"\n\n    # tags - (Optional) List of tags for this security rule.\n    tags = \"\"\n\n    # negate_source - (Optional) (Optional, bool) If the source should be negated.\n    negate_source = \"\"\n\n    # negate_destination - (Optional) (Optional, bool) If the destination should be negated.\n    negate_destination = \"\"\n\n    # action - (Optional) Action for the matched traffic.  This can be `allow`\n    action = \"\"\n\n    # log_setting - (Optional) Log forwarding profile.\n    log_setting = \"\"\n\n    # log_start - (Optional) (Optional, bool) Log the start of the traffic flow.\n    log_start = \"\"\n\n    # log_end - (Optional) (Optional, bool) Log the end of the traffic flow (default: `true`).\n    log_end = \"\"\n\n    # disabled - (Optional) (Optional, bool) Set to `true` to disable this rule.\n    disabled = \"\"\n\n    # schedule - (Optional) The security rule schedule.\n    schedule = \"\"\n\n    # icmp_unreachable - (Optional) Set to `true` to enable ICMP unreachable.\n    icmp_unreachable = \"\"\n\n    # disable_server_response_inspection - (Optional) Set to `true` to disable\n    disable_server_response_inspection = \"\"\n\n    # group - (Optional) Profile Setting: `Group` - The group profile name.\n    group = \"\"\n\n    # virus - (Optional) Profile Setting: `Profiles` - The antivirus setting.\n    virus = \"\"\n\n    # spyware - (Optional) Profile Setting: `Profiles` - The anti-spyware\n    spyware = \"\"\n\n    # vulnerability - (Optional) Profile Setting: `Profiles` - The Vulnerability\n    vulnerability = \"\"\n\n    # url_filtering - (Optional) Profile Setting: `Profiles` - The URL filtering\n    url_filtering = \"\"\n\n    # file_blocking - (Optional) Profile Setting: `Profiles` - The file blocking\n    file_blocking = \"\"\n\n    # wildfire_analysis - (Optional) Profile Setting: `Profiles` - The WildFire\n    wildfire_analysis = \"\"\n\n    # data_filtering - (Optional) Profile Setting: `Profiles` - The Data\n    data_filtering = \"\"\n\n    # target - (Optional) A target definition (see below).  If there are no\n    target = \"\"\n\n    # negate_target - (Optional) (Optional, bool) Instead of applying the rule for the\n    negate_target = \"\"\n\n    # vsys_list - (Optional) A subset of all available vsys on the firewall\n    vsys_list = \"\"\n\n    # Exported Attributes\n}",
        "description": "This resource allows you to manage the full security posture. ~> **Note:** `panos_panorama_security_policies` is known as `panos_panorama_security_policy`. This resource manages the full set of security rules, enforcing both the\ncontents of individual rules as well as their ordering.  Rules are defined in\na `rule` config block.  As this manages the full set of security rules for\na given rulebase, any extraneous rules are removed on `terraform apply`. For each security rule, there are three styles of profile settings: `None` (the default)  `Group`  `Profiles`  The Profile Setting is implicitly chosen based on what params are configured\nfor the security rule.  If you want a Profile Setting of `Group`, then the\n`group` param should be set to the desired Group Profile.  If you want a\nProfile Setting of `Profiles`, then you will need to specify one or more of\nthe following params: `virus`  `spyware`  `vulnerability`  `url_filtering`  `file_blocking`  `wildfire_analysis`  `data_filtering`  If the `group` param and none of the `Profiles` params are specified, then\nthe Profile Setting is set to `None`.",
        "prefix": "panos-panorama-resource-security-policy"
    },
    "panos-panorama-resource-security-rule-group": {
        "body": "resource \"panos_panorama_security_rule_group\" \"$1\" {\n    # serial - (Required) The serial number of the firewall.\n    serial = \"\"\n\n    # categories - (Required) List of categories.\n    categories = \"\"\n\n    # services - (Required) List of services.\n    services = \"\"\n\n    # applications - (Required) List of applications.\n    applications = \"\"\n\n    # destination_addresses - (Required) List of destination addresses.\n    destination_addresses = \"\"\n\n    # destination_zones - (Required) List of destination zones.\n    destination_zones = \"\"\n\n    # hip_profiles - (Required) List of HIP profiles.\n    hip_profiles = \"\"\n\n    # source_users - (Required) List of source users.\n    source_users = \"\"\n\n    # source_addresses - (Required) List of source addresses.\n    source_addresses = \"\"\n\n    # source_zones - (Required) List of source zones.\n    source_zones = \"\"\n\n    # name - (Required) The security rule name.\n    name = \"\"\n\n    # device_group - (Optional) The device group to put the security rules into\n    device_group = \"\"\n\n    # rulebase - (Optional) The rulebase.  This can be `pre-rulebase` (default),\n    rulebase = \"\"\n\n    # position_keyword - (Optional) A positioning keyword for this group.  This\n    position_keyword = \"\"\n\n    # bottom - (Optional) `bottom`, or left empty (the default) to have no particular placement.  This\n    bottom = \"\"\n\n    # position_reference - (Optional) Required if `position_keyword` is one of the\n    position_reference = \"\"\n\n    # rule - (Optional) The security rule definition (see below).  The security rule\n    rule = \"\"\n\n    # type - (Optional) Rule type.  This can be `universal` (default),\n    type = \"\"\n\n    # interzone - (Optional) `interzone`, or `intrazone`.\n    interzone = \"\"\n\n    # description - (Optional) The description.\n    description = \"\"\n\n    # tags - (Optional) List of tags for this security rule.\n    tags = \"\"\n\n    # negate_source - (Optional) (Optional, bool) If the source should be negated.\n    negate_source = \"\"\n\n    # negate_destination - (Optional) (Optional, bool) If the destination should be negated.\n    negate_destination = \"\"\n\n    # action - (Optional) Action for the matched traffic.  This can be `allow`\n    action = \"\"\n\n    # log_setting - (Optional) Log forwarding profile.\n    log_setting = \"\"\n\n    # log_start - (Optional) (Optional, bool) Log the start of the traffic flow.\n    log_start = \"\"\n\n    # log_end - (Optional) (Optional, bool) Log the end of the traffic flow (default: `true`).\n    log_end = \"\"\n\n    # disabled - (Optional) (Optional, bool) Set to `true` to disable this rule.\n    disabled = \"\"\n\n    # schedule - (Optional) The security rule schedule.\n    schedule = \"\"\n\n    # icmp_unreachable - (Optional) Set to `true` to enable ICMP unreachable.\n    icmp_unreachable = \"\"\n\n    # disable_server_response_inspection - (Optional) Set to `true` to disable\n    disable_server_response_inspection = \"\"\n\n    # group - (Optional) Profile Setting: `Group` - The group profile name.\n    group = \"\"\n\n    # virus - (Optional) Profile Setting: `Profiles` - The antivirus setting.\n    virus = \"\"\n\n    # spyware - (Optional) Profile Setting: `Profiles` - The anti-spyware\n    spyware = \"\"\n\n    # vulnerability - (Optional) Profile Setting: `Profiles` - The Vulnerability\n    vulnerability = \"\"\n\n    # url_filtering - (Optional) Profile Setting: `Profiles` - The URL filtering\n    url_filtering = \"\"\n\n    # file_blocking - (Optional) Profile Setting: `Profiles` - The file blocking\n    file_blocking = \"\"\n\n    # wildfire_analysis - (Optional) Profile Setting: `Profiles` - The WildFire\n    wildfire_analysis = \"\"\n\n    # data_filtering - (Optional) Profile Setting: `Profiles` - The Data\n    data_filtering = \"\"\n\n    # target - (Optional) A target definition (see below).  If there are no\n    target = \"\"\n\n    # negate_target - (Optional) (Optional, bool) Instead of applying the rule for the\n    negate_target = \"\"\n\n    # vsys_list - (Optional) A subset of all available vsys on the firewall\n    vsys_list = \"\"\n\n    # Exported Attributes\n}",
        "description": "This resource allows you to add/update/delete Panorama security rule groups. ~> **Note:** `panos_panorama_security_policy_group` is known as `panos_panorama_security_rule_group`. This resource manages clusters of security rules in a single device group,\nenforcing both the contents of individual rules as well as their\nordering.  Rules are defined in a `rule` config block. Because this resource only manages what it's told to, it will not manage\nany rules that may already exist on Panorama.  This has\nimplications on the effective security posture of Panorama, but it\nwill allow you to spread your security rules across multiple Terraform\nstate files.  If you want to verify that the security rules are only\nwhat appears in the plan file, then you should probably be using the\n[panos_panorama_security_policy](panorama_security_policy.html) resource. Although you cannot modify non-group security rules with this\nresource, the `position_keyword` and `position_reference` parameters allow you\nto reference some other security rule that already exists, using it as\na means to ensure some rough placement within the ruleset as a whole. For each security rule, there are three styles of profile settings: `None` (the default)  `Group`  `Profiles`  The Profile Setting is implicitly chosen based on what params are configured\nfor the security rule.  If you want a Profile Setting of `Group`, then the\n`group` param should be set to the desired Group Profile.  If you want a\nProfile Setting of `Profiles`, then you will need to specify one or more of\nthe following params: `virus`  `spyware`  `vulnerability`  `url_filtering`  `file_blocking`  `wildfire_analysis`  `data_filtering`  If the `group` param and none of the `Profiles` params are specified, then\nthe Profile Setting is set to `None`.",
        "prefix": "panos-panorama-resource-security-rule-group"
    },
    "panos-panorama-resource-service-group": {
        "body": "resource \"panos_panorama_service_group\" \"$1\" {\n    # services - (Required) List of services to put in this service group.\n    services = \"\"\n\n    # name - (Required) The service group's name.\n    name = \"\"\n\n    # device_group - (Optional) The device group to put the service group into\n    device_group = \"\"\n\n    # tags - (Optional) List of administrative tags.\n    tags = \"\"\n\n    # Exported Attributes\n}",
        "description": "This resource allows you to add/update/delete Panorama service groups.",
        "prefix": "panos-panorama-resource-service-group"
    },
    "panos-panorama-resource-service-object": {
        "body": "resource \"panos_panorama_service_object\" \"$1\" {\n    # destination_port - (Required) The destination port.  This can be a single\n    destination_port = \"\"\n\n    # protocol - (Required) The service's protocol.  This should be `tcp` or\n    protocol = \"\"\n\n    # name - (Required) The service object's name.\n    name = \"\"\n\n    # device_group - (Optional) The device group to put the service object into\n    device_group = \"\"\n\n    # description - (Optional) The service object's description.\n    description = \"\"\n\n    # udp - (Optional) `udp`.\n    udp = \"\"\n\n    # source_port - (Optional) The source port.  This can be a single port\n    source_port = \"\"\n\n    # tags - (Optional) List of administrative tags.\n    tags = \"\"\n\n    # Exported Attributes\n}",
        "description": "This resource allows you to add/update/delete Panorama service objects.",
        "prefix": "panos-panorama-resource-service-object"
    },
    "panos-panorama-resource-static-route-ipv4": {
        "body": "resource \"panos_panorama_static_route_ipv4\" \"$1\" {\n    # destination - (Required) Destination IP address / prefix.\n    destination = \"\"\n\n    # virtual_router - (Required) The virtual router to add the static\n    virtual_router = \"\"\n\n    # name - (Required) The address object's name.\n    name = \"\"\n\n    # template - (Optional) The template name.\n    template = \"\"\n\n    # template_stack - (Optional) The template stack name.\n    template_stack = \"\"\n\n    # interface - (Optional) Interface to use.\n    interface = \"\"\n\n    # type - (Optional) The next hop type.  Valid values are `ip-address` (the\n    type = \"\"\n\n    # next_hop - (Optional) The value for the `type` setting.\n    next_hop = \"\"\n\n    # admin_distance - (Optional) The admin distance.\n    admin_distance = \"\"\n\n    # metric - (Optional) (Optional, int) Metric value / path cost (default: `10`).\n    metric = \"\"\n\n    # route_table - (Optional) Target routing table to install the route.  Valid\n    route_table = \"\"\n\n    # bfd_profile - (Optional) (Optional, PAN-OS 7.1+) BFD configuration.\n    bfd_profile = \"\"\n\n    # Exported Attributes\n}",
        "description": "This resource allows you to add/update/delete Panorama IPv4 static routes on a\nvirtual router for either a template or a template stack.",
        "prefix": "panos-panorama-resource-static-route-ipv4"
    },
    "panos-panorama-resource-template": {
        "body": "resource \"panos_panorama_template\" \"$1\" {\n    # serial - (Required) The serial number of the firewall.\n    serial = \"\"\n\n    # name - (Required) The template's name.\n    name = \"\"\n\n    # description - (Optional) The template's description.\n    description = \"\"\n\n    # device - (Optional) The device definition (see below).\n    device = \"\"\n\n    # vsys_list - (Optional) A subset of all available vsys on the firewall\n    vsys_list = \"\"\n\n    # Exported Attributes\n}",
        "description": "This resource allows you to add/update/delete Panorama templates. This resource has some overlap with the `panos_panorama_template_entry`\nresource.  If you want to use this resource with the other one, then make\nsure that your `panos_panorama_template` spec does not define any\n`device` blocks, and just stays as \"computed\". This is the appropriate resource to use if `terraform destroy` should delete\nthe template. **Note** - In PAN-OS 8.1, it looks like the `devices` field has\nbeen removed.  Creating a template stack and specifying devices in the template\nstack is still present in PAN-OS 8.1.",
        "prefix": "panos-panorama-resource-template"
    },
    "panos-panorama-resource-template-entry": {
        "body": "resource \"panos_panorama_template_entry\" \"$1\" {\n    # serial - (Required) The serial number of the firewall.\n    serial = \"\"\n\n    # template - (Required) The template name.\n    template = \"\"\n\n    # vsys_list - (Optional) A subset of all available vsys on the firewall\n    vsys_list = \"\"\n\n    # Exported Attributes\n}",
        "description": "This resource allows you to add/update/delete a specific device in a Panorama\ntemplate. This resource has some overlap with the `panos_panorama_template`\nresource.  If you want to use this resource with the other one, then make\nsure that your `panos_panorama_template` spec does not define any\n`device` blocks, and just stays as \"computed\". This is the appropriate resource to use if you have a pre-existing template\nin Panorama and don't want Terraform to delete it on `terraform destroy`. An interesting side effect of the underlying XML API - if the template does\nnot already exist, then this resource can actually create it.  However, since\nonly the single entry for the specific serial number is deleted, then a\n`terraform destroy` would not remove the template itself in this situation.",
        "prefix": "panos-panorama-resource-template-entry"
    },
    "panos-panorama-resource-template-stack": {
        "body": "resource \"panos_panorama_template_stack\" \"$1\" {\n    # name - (Required) The stack's name.\n    name = \"\"\n\n    # description - (Optional) The stack's description.\n    description = \"\"\n\n    # default_vsys - (Optional) The default virtual system template configuration\n    default_vsys = \"\"\n\n    # templates - (Optional) List of templates in this stack.\n    templates = \"\"\n\n    # devices - (Optional) List of serial numbers to include in this stack.\n    devices = \"\"\n\n    # Exported Attributes\n}",
        "description": "This resource allows you to add/update/delete Panorama template stacks. This resource has some overlap with the `panos_panorama_template_stack_entry`\nresource.  If you want to use this resource with the other one, then make\nsure that your `panos_panorama_template_stack` spec does not define any\n`device` blocks, and just stays as \"computed\". This is the appropriate resource to use if `terraform destroy` should delete\nthe template stack.",
        "prefix": "panos-panorama-resource-template-stack"
    },
    "panos-panorama-resource-template-stack-entry": {
        "body": "resource \"panos_panorama_template_stack_entry\" \"$1\" {\n    # device - (Required) The serial number of the device to add.\n    device = \"\"\n\n    # template - (Required) The template name.\n    template = \"\"\n\n    # Exported Attributes\n}",
        "description": "This resource allows you to add/update/delete a specific device in a Panorama\ntemplate stack. This resource has some overlap with the `panos_panorama_template_stack`\nresource.  If you want to use this resource with the other one, then make\nsure that your `panos_panorama_template_stack` spec does not define the\n`devices` field. This is the appropriate resource to use if you have a pre-existing template stack\nin Panorama and don't want Terraform to delete it on `terraform destroy`.",
        "prefix": "panos-panorama-resource-template-stack-entry"
    },
    "panos-panorama-resource-template-variable": {
        "body": "resource \"panos_panorama_template_variable\" \"$1\" {\n    # value - (Required) The variable value.\n    value = \"\"\n\n    # name - (Required) The template's name.  This must start with a dollar sign ($).\n    name = \"\"\n\n    # template - (Optional) The template name.\n    template = \"\"\n\n    # template_stack - (Optional) The template stack name.\n    template_stack = \"\"\n\n    # type - (Optional) The variable type.  Valid values are `ip-netmask`\n    type = \"\"\n\n    # Exported Attributes\n}",
        "description": "This resource allows you to add/update/delete variables for both Panorama\ntemplates and template stacks. Template variables are available in PAN-OS 8.1+.",
        "prefix": "panos-panorama-resource-template-variable"
    },
    "panos-panorama-resource-tunnel-interface": {
        "body": "resource \"panos_panorama_tunnel_interface\" \"$1\" {\n    # template - (Required) The template name.\n    template = \"\"\n\n    # name - (Required) The interface's name.  This must start with `tunnel.`.\n    name = \"\"\n\n    # vsys - (Optional) The vsys that will use this interface (default: `vsys1`).\n    vsys = \"\"\n\n    # comment - (Optional) The interface comment.\n    comment = \"\"\n\n    # netflow_profile - (Optional) The netflow profile.\n    netflow_profile = \"\"\n\n    # static_ips - (Optional) List of static IPv4 addresses to set for this data\n    static_ips = \"\"\n\n    # management_profile - (Optional) The management profile.\n    management_profile = \"\"\n\n    # mtu - (Optional) The MTU.\n    mtu = \"\"\n\n    # Exported Attributes\n}",
        "description": "This resource allows you to add/update/delete Panorama tunnel interfaces\nfor templates.",
        "prefix": "panos-panorama-resource-tunnel-interface"
    },
    "panos-panorama-resource-virtual-router": {
        "body": "resource \"panos_panorama_virtual_router\" \"$1\" {\n    # vsys - (Required) The vsys that will use this virtual router.  This should\n    vsys = \"\"\n\n    # template - (Required) The template name.\n    template = \"\"\n\n    # name - (Required) The virtual router's name.\n    name = \"\"\n\n    # interfaces - (Optional) List of interfaces that should use this virtual\n    interfaces = \"\"\n\n    # static_dist - (Optional) Admin distance - Static (default: `10`).\n    static_dist = \"\"\n\n    # ospf_int_dist - (Optional) Admin distance - OSPF Int (default: `30`).\n    ospf_int_dist = \"\"\n\n    # ospf_ext_dist - (Optional) Admin distance - OSPF Ext (default: `110`).\n    ospf_ext_dist = \"\"\n\n    # ibgp_dist - (Optional) Admin distance - IBGP (default: `200`).\n    ibgp_dist = \"\"\n\n    # ebgp_dist - (Optional) Admin distance - EBGP (default: `20`).\n    ebgp_dist = \"\"\n\n    # rip_dist - (Optional) Admin distance - RIP (default: `120`).\n    rip_dist = \"\"\n\n    # Exported Attributes\n}",
        "description": "This resource allows you to add/update/delete Panorama virtual routers\nfor templates. **Note** - The `default` virtual router may be configured with this resource,\nhowever it will not be deleted from Panorama.  It will only be unexported\nfrom the vsys that it is currently imported in, and any interfaces imported\ninto the virtual router will be removed. This resource has some overlap with the `panos_panorama_virtual_router_entry`\nresource.  If you want to use this resource with the other one, then make\nsure that your `panos_panorama_virtual_router` spec does not define the\n`interfaces` field.",
        "prefix": "panos-panorama-resource-virtual-router"
    },
    "panos-panorama-resource-virtual-router-entry": {
        "body": "resource \"panos_panorama_virtual_router_entry\" \"$1\" {\n    # interface - (Required) The interface to import into the virtual router.\n    interface = \"\"\n\n    # virtual_router - (Required) The virtual router's name.\n    virtual_router = \"\"\n\n    # template - (Required) The template name.\n    template = \"\"\n\n    # Exported Attributes\n}",
        "description": "This resource allows you to add/update/delete an interface in a Panorama\nvirtual router template. This resource has some overlap with the `panos_panorama_virtual_router`\nresource.  If you want to use this resource with the other one, then make\nsure that your `panos_panorama_virtual_router` spec does not define the\n`interfaces` field.",
        "prefix": "panos-panorama-resource-virtual-router-entry"
    },
    "panos-panorama-resource-vlan-interface": {
        "body": "resource \"panos_panorama_vlan_interface\" \"$1\" {\n    # template - (Required) The template name.\n    template = \"\"\n\n    # name - (Required) The interface's name.  Must start with `vlan.`.\n    name = \"\"\n\n    # vsys - (Optional) The vsys that will use this interface (default: `vsys1`).\n    vsys = \"\"\n\n    # comment - (Optional) The interface comment.\n    comment = \"\"\n\n    # netflow_profile - (Optional) The netflow profile.\n    netflow_profile = \"\"\n\n    # static_ips - (Optional) List of static IPv4 addresses to set for this data\n    static_ips = \"\"\n\n    # enable_dhcp - (Optional) Set to `true` to enable DHCP on this interface.\n    enable_dhcp = \"\"\n\n    # create_dhcp_default_route - (Optional) Set to `true` to create a DHCP\n    create_dhcp_default_route = \"\"\n\n    # dhcp_default_route_metric - (Optional) The metric for the DHCP default\n    dhcp_default_route_metric = \"\"\n\n    # management_profile - (Optional) The management profile.\n    management_profile = \"\"\n\n    # mtu - (Optional) The MTU.\n    mtu = \"\"\n\n    # adjust_tcp_mss - (Optional) Adjust TCP MSS (default: false).\n    adjust_tcp_mss = \"\"\n\n    # Exported Attributes\n}",
        "description": "This resource allows you to add/update/delete Panorama VLAN interfaces\nfor templates.",
        "prefix": "panos-panorama-resource-vlan-interface"
    },
    "panos-panorama-resource-zone": {
        "body": "resource \"panos_panorama_zone\" \"$1\" {\n    # mode - (Required) The zone's mode.  This can be `layer3`, `layer2`,\n    mode = \"\"\n\n    # name - (Required) The zone's name.\n    name = \"\"\n\n    # template - (Optional) The template name.\n    template = \"\"\n\n    # template_stack - (Optional) The template stack name.\n    template_stack = \"\"\n\n    # vsys - (Optional) The vsys to put the zone into (default: `vsys1`).\n    vsys = \"\"\n\n    # zone_profile - (Optional) The zone protection profile.\n    zone_profile = \"\"\n\n    # log_setting - (Optional) Log setting.\n    log_setting = \"\"\n\n    # enable_user_id - (Optional) Boolean to enable user identification.\n    enable_user_id = \"\"\n\n    # interfaces - (Optional) List of interfaces to associated with this zone.\n    interfaces = \"\"\n\n    # include_acls - (Optional) Users from these addresses/subnets will\n    include_acls = \"\"\n\n    # exclude_acls - (Optional) Users from these addresses/subnets will not\n    exclude_acls = \"\"\n\n    # Exported Attributes\n}",
        "description": "This resource allows you to add/update/delete zones on Panorama for both\ntemplates and template stacks. This resource has some overlap with the `panos_panorama_zone_entry`\nresource.  If you want to use this resource with the other one, then make\nsure that your `panos_panorama_zone` spec does not define the\n`interfaces` field.",
        "prefix": "panos-panorama-resource-zone"
    },
    "panos-panorama-resource-zone-entry": {
        "body": "resource \"panos_panorama_zone_entry\" \"$1\" {\n    # interface - (Required) The interface's name.\n    interface = \"\"\n\n    # zone - (Required) The zone's name.\n    zone = \"\"\n\n    # template - (Required) The template name.\n    template = \"\"\n\n    # vsys - (Optional) The vsys (default: `vsys1`).\n    vsys = \"\"\n\n    # mode - (Optional) The mode.  Can be `layer3` (default), `layer2`,\n    mode = \"\"\n\n    # Exported Attributes\n}",
        "description": "This resource allows you to add/update/delete a specific interface in a Panorama\nzone. This resource has some overlap with the `panos_panorama_zone`\nresource.  If you want to use this resource with the other one, then make\nsure that your `panos_panorama_zone` spec does not define the\n`interfaces` field. This is the appropriate resource to use if you have a pre-existing zone\nin Panorama and don't want Terraform to delete it on `terraform destroy`.",
        "prefix": "panos-panorama-resource-zone-entry"
    },
    "panos-resource-address-group": {
        "body": "resource \"panos_address_group\" \"$1\" {\n    # name - (Required) The address group's name.\n    name = \"\"\n\n    # vsys - (Optional) The vsys to put the address group into (default:\n    vsys = \"\"\n\n    # static_addresses - (Optional) The address objects to include in this\n    static_addresses = \"\"\n\n    # dynamic_match - (Optional) The IP tags to include in this DAG.\n    dynamic_match = \"\"\n\n    # description - (Optional) The address group's description.\n    description = \"\"\n\n    # tags - (Optional) List of administrative tags.\n    tags = \"\"\n\n    # Exported Attributes\n}",
        "description": "This resource allows you to add/update/delete address groups. Address groups are either statically defined or dynamically defined, so only\n`static_addresses` or `dynamic_match` should be defined within a given address\ngroup.",
        "prefix": "panos-resource-address-group"
    },
    "panos-resource-address-object": {
        "body": "resource \"panos_address_object\" \"$1\" {\n    # value - (Required) The address object's value.  This can take various\n    value = \"\"\n\n    # name - (Required) The address object's name.\n    name = \"\"\n\n    # vsys - (Optional) The vsys to put the address object into (default:\n    vsys = \"\"\n\n    # type - (Optional) The type of address object.  This can be `ip-netmask`\n    type = \"\"\n\n    # description - (Optional) The address object's description.\n    description = \"\"\n\n    # tags - (Optional) List of administrative tags.\n    tags = \"\"\n\n    # Exported Attributes\n}",
        "description": "This resource allows you to add/update/delete address objects.",
        "prefix": "panos-resource-address-object"
    },
    "panos-resource-administrative-tag": {
        "body": "resource \"panos_administrative_tag\" \"$1\" {\n    # name - (Required) The administrative tag's name.\n    name = \"\"\n\n    # vsys - (Optional) The vsys to put the administrative tag into (default:\n    vsys = \"\"\n\n    # color - (Optional) The tag's color.  This should be either an empty string\n    color = \"\"\n\n    # comment - (Optional) The administrative tag's description.\n    comment = \"\"\n\n    # Exported Attributes\n}",
        "description": "This resource allows you to add/update/delete administrative tags.",
        "prefix": "panos-resource-administrative-tag"
    },
    "panos-resource-dag-tags": {
        "body": "resource \"panos_dag_tags\" \"$1\" {\n    # register - (Required) A set that includes `ip`, the IP address to be tagged\n    register = \"\"\n\n    # vsys - (Optional) The vsys to put the DAG tags in (default: `vsys1`).\n    vsys = \"\"\n\n    # Exported Attributes\n}",
        "description": "This resource allows you to add and remove dynamic address group tags. The `ip` field should be unique in the `panos_dag_tags` block, and there\nshould only be one `panos_dag_tags` block defined in a given plan. **Note** - Tags are only removed during `terraform destroy`.  Updating an\napplied terraform plan to have alternative tags will leave behind the\nold tags from the previously published plan(s).",
        "prefix": "panos-resource-dag-tags"
    },
    "panos-resource-edl": {
        "body": "resource \"panos_panorama_edl\" \"$1\" {\n    # name - (Required) The object's name\n    name = \"\"\n\n    # device_group - (Optional) The device group (default: `shared`)\n    device_group = \"\"\n\n    # type - (Optional) The type of EDL.  This can be `ip` (the default; and the\n    type = \"\"\n\n    # description - (Optional) The object's description.\n    description = \"\"\n\n    # source - (Optional) The EDL source URL\n    source = \"\"\n\n    # certificate_profile - (Optional) Profile for authenticating client certificates\n    certificate_profile = \"\"\n\n    # username - (Optional) EDL username\n    username = \"\"\n\n    # password - (Optional) EDL password\n    password = \"\"\n\n    # repeat - (Optional) How often to retrieve the EDL.  This can be `hourly` (the\n    repeat = \"\"\n\n    # repeat_at - (Optional) The time at which to retrieve the EDL.  Please refer\n    repeat_at = \"\"\n\n    # repeat_day_of_week - (Optional) If `repeat` is `weekly`, then this should\n    repeat_day_of_week = \"\"\n\n    # monday - (Optional) `monday`, `tuesday`, `wednesday`, `thursday`, `friday`, `saturday`, and\n    monday = \"\"\n\n    # sunday - (Optional) `sunday`\n    sunday = \"\"\n\n    # repeat_day_of_month - (Optional) (Optional, int) If `repeat` is `monthly`, then this should\n    repeat_day_of_month = \"\"\n\n    # exceptions - (Optional) (Optional, list) Provide a list of exception entries.\n    exceptions = \"\"\n\n    # Exported Attributes\n}",
        "description": "This resource allows you to add/update/delete Panorama external dynamic lists (EDL).",
        "prefix": "panos-resource-edl"
    },
    "panos-resource-ethernet-interface": {
        "body": "resource \"panos_ethernet_interface\" \"$1\" {\n    # mode - (Required) The interface mode.  This can be any of the following\n    mode = \"\"\n\n    # vsys - (Required) The vsys that will use this interface.  This should be\n    vsys = \"\"\n\n    # name - (Required) The ethernet interface's name.  This should be something\n    name = \"\"\n\n    # static_ips - (Optional) List of static IPv4 addresses to set for this data\n    static_ips = \"\"\n\n    # enable_dhcp - (Optional) Set to `true` to enable DHCP on this interface.\n    enable_dhcp = \"\"\n\n    # create_dhcp_default_route - (Optional) Set to `true` to create a DHCP\n    create_dhcp_default_route = \"\"\n\n    # dhcp_default_route_metric - (Optional) The metric for the DHCP default\n    dhcp_default_route_metric = \"\"\n\n    # management_profile - (Optional) The management profile.\n    management_profile = \"\"\n\n    # mtu - (Optional) The MTU.\n    mtu = \"\"\n\n    # adjust_tcp_mss - (Optional) Adjust TCP MSS (default: false).\n    adjust_tcp_mss = \"\"\n\n    # netflow_profile - (Optional) The netflow profile.\n    netflow_profile = \"\"\n\n    # lldp_enabled - (Optional) Enable LLDP (default: false).\n    lldp_enabled = \"\"\n\n    # lldp_profile - (Optional) LLDP profile.\n    lldp_profile = \"\"\n\n    # link_speed - (Optional) Link speed.  This can be any of the following:\n    link_speed = \"\"\n\n    # link_duplex - (Optional) Link duplex setting.  This can be `full`, `half`,\n    link_duplex = \"\"\n\n    # link_state - (Optional) The link state.  This can be `up`, `down`, or\n    link_state = \"\"\n\n    # auto - (Optional) `auto`.\n    auto = \"\"\n\n    # aggregate_group - (Optional) The aggregate group (applicable for\n    aggregate_group = \"\"\n\n    # comment - (Optional) The interface comment.\n    comment = \"\"\n\n    # Exported Attributes\n}",
        "description": "This resource allows you to add/update/delete ethernet interfaces.",
        "prefix": "panos-resource-ethernet-interface"
    },
    "panos-resource-general-settings": {
        "body": "resource \"panos_general_settings\" \"$1\" {\n    # hostname - (Optional) Firewall hostname.\n    hostname = \"\"\n\n    # timezone - (Optional) `US/Pacific`).\n    timezone = \"\"\n\n    # domain - (Optional) The domain.\n    domain = \"\"\n\n    # update_server - (Optional) The update server (Default: `updates.paloaltonetworks.com`).\n    update_server = \"\"\n\n    # verify_update_server - (Optional) Verify update server identity (Default: `true`).\n    verify_update_server = \"\"\n\n    # dns_primary - (Optional) Primary DNS server.\n    dns_primary = \"\"\n\n    # dns_secondary - (Optional) Secondary DNS server.\n    dns_secondary = \"\"\n\n    # ntp_primary_address - (Optional) Primary NTP server.\n    ntp_primary_address = \"\"\n\n    # ntp_primary_auth_type - (Optional) Primary NTP auth type.  This can be `none`,\n    ntp_primary_auth_type = \"\"\n\n    # autokey - (Optional) `autokey`, or `symmetric-key`.\n    autokey = \"\"\n\n    # ntp_primary_key_id - (Optional) Primary NTP `symmetric-key` key ID.\n    ntp_primary_key_id = \"\"\n\n    # ntp_primary_algorithm - (Optional) Primary NTP `symmetric-key` algorithm.  This can be\n    ntp_primary_algorithm = \"\"\n\n    # ntp_primary_auth_key - (Optional) Primary NTP `symmetric-key` auth key.  This is the\n    ntp_primary_auth_key = \"\"\n\n    # ntp_secondary_address - (Optional) Secondary NTP server.\n    ntp_secondary_address = \"\"\n\n    # ntp_secondary_auth_type - (Optional) Secondary NTP auth type.  This can be `none`,\n    ntp_secondary_auth_type = \"\"\n\n    # autokey - (Optional) `autokey`, or `symmetric-key`.\n    autokey = \"\"\n\n    # ntp_secondary_key_id - (Optional) Secondary NTP `symmetric-key` key ID.\n    ntp_secondary_key_id = \"\"\n\n    # ntp_secondary_algorithm - (Optional) Secondary NTP `symmetric-key` algorithm.  This\n    ntp_secondary_algorithm = \"\"\n\n    # ntp_secondary_auth_key - (Optional) Secondary NTP `symmetric-key` auth key.  This is\n    ntp_secondary_auth_key = \"\"\n\n    # Exported Attributes\n}",
        "description": "This resource allows you to update the general device settings, such as DNS\nor the hostname. All params are optional for this resource.  If any options are not specified,\nthen whatever is already configured on the firewall is left as-is.  The\ngeneral device settings will always exist on the firewall, so `terraform\ndestroy` does not remove config from the firewall.",
        "prefix": "panos-resource-general-settings"
    },
    "panos-resource-ike-crypto-profile": {
        "body": "resource \"panos_ike_crypto_profile\" \"$1\" {\n    # name - (Required) The object's name\n    name = \"\"\n\n    # dh_groups - (Optional) (Required, list) List of DH Group entries.  Values should\n    dh_groups = \"\"\n\n    # authentications - (Optional) (Required, list) List of authentication types.  This c\n    authentications = \"\"\n\n    # encryptions - (Optional) (Required, list) List of encryption types.  Valid values\n    encryptions = \"\"\n\n    # lifetime_type - (Optional) The lifetime type.  Valid values are `seconds`,\n    lifetime_type = \"\"\n\n    # minutes - (Optional) `minutes`, `hours` (the default), and `days`.\n    minutes = \"\"\n\n    # lifetime_value - (Optional) (Optional, int) The lifetime value.\n    lifetime_value = \"\"\n\n    # authentication_multiple - (Optional) (Optional, PAN-OS 7.0+, int) IKEv2 SA\n    authentication_multiple = \"\"\n\n    # Exported Attributes\n}",
        "description": "This resource allows you to add/update/delete IKE crypto profiles.",
        "prefix": "panos-resource-ike-crypto-profile"
    },
    "panos-resource-ike-gateway": {
        "body": "resource \"panos_ike_gateway\" \"$1\" {\n    # interface - (Required) The interface.\n    interface = \"\"\n\n    # name - (Required) The object's name\n    name = \"\"\n\n    # version - (Optional) (Optional, PAN-OS 7.0+) The IKE gateway version.  Valid values are\n    version = \"\"\n\n    # disabled - (Optional) (Optional, PAN-OS 7.0+, bool) Set to `true` to disable.\n    disabled = \"\"\n\n    # peer_ip_type - (Optional) The peer IP type.  Valid values are `ip`,\n    peer_ip_type = \"\"\n\n    # dynamic - (Optional) `dynamic`, and `fqdn` (PANOS 8.1+).\n    dynamic = \"\"\n\n    # peer_ip_value - (Optional) The peer IP value.\n    peer_ip_value = \"\"\n\n    # local_ip_address_type - (Optional) The local IP address type.  Valid\n    local_ip_address_type = \"\"\n\n    # local_ip_address_value - (Optional) The IP address if `local_ip_address_type`\n    local_ip_address_value = \"\"\n\n    # auth_type - (Optional) The auth type.  Valid values are `pre-shared-key`\n    auth_type = \"\"\n\n    # pre_shared_key - (Optional) The pre-shared key value.\n    pre_shared_key = \"\"\n\n    # local_id_type - (Optional) The local ID type.  Valid values are `ipaddr`,\n    local_id_type = \"\"\n\n    # fqdn - (Optional) `fqdn`, `ufqdn`, `keyid`, or `dn`.\n    fqdn = \"\"\n\n    # local_id_value - (Optional) The local ID value.\n    local_id_value = \"\"\n\n    # peer_id_type - (Optional) The peer ID type.  Valid values are `ipaddr`,\n    peer_id_type = \"\"\n\n    # fqdn - (Optional) `fqdn`, `ufqdn`, `keyid`, or `dn`.\n    fqdn = \"\"\n\n    # peer_id_value - (Optional) The peer ID value.\n    peer_id_value = \"\"\n\n    # peer_id_check - (Optional) Enable peer ID wildcard match for certificate\n    peer_id_check = \"\"\n\n    # local_cert - (Optional) The local certificate name.\n    local_cert = \"\"\n\n    # cert_enable_hash_and_url - (Optional) (Optional, PAN-OS 7.0+, bool) Set to `true` to use\n    cert_enable_hash_and_url = \"\"\n\n    # cert_base_url - (Optional) The host and directory part of URL for local\n    cert_base_url = \"\"\n\n    # cert_use_management_as_source - (Optional) (Optional, PAN-OS 7.0+, bool) Set to `true` to\n    cert_use_management_as_source = \"\"\n\n    # cert_permit_payload_mismatch - (Optional) (Optional, bool) Set to `true` to permit\n    cert_permit_payload_mismatch = \"\"\n\n    # cert_profile - (Optional) Profile for certificate valdiation during IKE\n    cert_profile = \"\"\n\n    # cert_enable_strict_validation - (Optional) (Optional, bool) Set to `true` to enable\n    cert_enable_strict_validation = \"\"\n\n    # enable_passive_mode - (Optional) (Optional, bool) Set to `true` to enable passive\n    enable_passive_mode = \"\"\n\n    # enable_nat_traversal - (Optional) (Optional, bool) Set to `true` to enable NAT\n    enable_nat_traversal = \"\"\n\n    # nat_traversal_keep_alive - (Optional) (Optional, int) Sending interval for NAT\n    nat_traversal_keep_alive = \"\"\n\n    # nat_traversal_enable_udp_checksum - (Optional) (Optional, bool) Set to `true` to enable\n    nat_traversal_enable_udp_checksum = \"\"\n\n    # enable_fragmentation - (Optional) (Optional, bool) Set to `true` to enable fragmentation.\n    enable_fragmentation = \"\"\n\n    # enable_dead_peer_detection - (Optional) (Optional, bool) Set to `true` to enable dead\n    enable_dead_peer_detection = \"\"\n\n    # dead_peer_detection_interval - (Optional) (Optional, int) The dead peer detection interval.\n    dead_peer_detection_interval = \"\"\n\n    # dead_peer_detection_retry - (Optional) (Optional, int) Number of retries before disconnection.\n    dead_peer_detection_retry = \"\"\n\n    # enable_liveness_check - (Optional) (Optional, , PAN-OS 7.0+bool) Set to `true` to\n    enable_liveness_check = \"\"\n\n    # liveness_check_interval - (Optional) (Optional, , PAN-OS 7.0+int) Delay interval before\n    liveness_check_interval = \"\"\n\n    # Exported Attributes\n}",
        "description": "This resource allows you to add/update/delete IKE gateways.",
        "prefix": "panos-resource-ike-gateway"
    },
    "panos-resource-ipsec-crypto-profile": {
        "body": "resource \"panos_ipsec_crypto_profile\" \"$1\" {\n    # name - (Required) The object's name\n    name = \"\"\n\n    # protocol - (Optional) The protocol.  Valid values are `esp` (the default)\n    protocol = \"\"\n\n    # authentications - (Optional) List of authentication types.\n    authentications = \"\"\n\n    # encryptions - (Optional) List of encryption types.  Valid values\n    encryptions = \"\"\n\n    # dh_group - (Optional) The DH group value.  Valid values should start with\n    dh_group = \"\"\n\n    # lifetime_type - (Optional) The lifetime type.  Valid values are `seconds`,\n    lifetime_type = \"\"\n\n    # minutes - (Optional) `minutes`, `hours` (the default), or `days`.\n    minutes = \"\"\n\n    # lifetime_value - (Optional) (Optional, int) The lifetime value.\n    lifetime_value = \"\"\n\n    # lifesize_type - (Optional) The lifesize type.  Valid values are `kb`, `mb`,\n    lifesize_type = \"\"\n\n    # gb - (Optional) `gb`, or `tb`.\n    gb = \"\"\n\n    # lifesize_value - (Optional) (Optional, int) the lifesize value.\n    lifesize_value = \"\"\n\n    # Exported Attributes\n}",
        "description": "This resource allows you to add/update/delete IPSec crypto profiles.",
        "prefix": "panos-resource-ipsec-crypto-profile"
    },
    "panos-resource-ipsec-tunnel": {
        "body": "resource \"panos_ipsec_tunnel\" \"$1\" {\n    # tunnel_interface - (Required) The tunnel interface.\n    tunnel_interface = \"\"\n\n    # name - (Required) The object's name\n    name = \"\"\n\n    # anti_replay - (Optional) (Optional, bool) Set to `true` to enable Anti-Replay check\n    anti_replay = \"\"\n\n    # copy_tos - (Optional) (Optional, bool) Set to `true` to copy IP TOS bits from inner\n    copy_tos = \"\"\n\n    # copy_flow_label - (Optional) (Optional, PAN-OS 7.0+, bool) Set to `true` to copy IPv6\n    copy_flow_label = \"\"\n\n    # disabled - (Optional) (Optional, PAN-OS 7.0+, bool) Set to `true` to disable this\n    disabled = \"\"\n\n    # type - (Optional) The type.  Valid values are `auto-key` (the default),\n    type = \"\"\n\n    # ak_ike_gateway - (Optional) IKE gateway name.\n    ak_ike_gateway = \"\"\n\n    # ak_ipsec_crypto_profile - (Optional) IPSec crypto profile name.\n    ak_ipsec_crypto_profile = \"\"\n\n    # mk_local_spi - (Optional) Outbound SPI, hex format.\n    mk_local_spi = \"\"\n\n    # mk_remote_spi - (Optional) Inbound SPI, hex format.\n    mk_remote_spi = \"\"\n\n    # mk_local_address_ip - (Optional) Specify exact IP address if interface\n    mk_local_address_ip = \"\"\n\n    # mk_local_address_floating_ip - (Optional) Floating IP address in HA\n    mk_local_address_floating_ip = \"\"\n\n    # mk_protocol - (Optional) Manual key protocol.  Valid valies are\n    mk_protocol = \"\"\n\n    # esp - (Optional) `esp` or `ah`.\n    esp = \"\"\n\n    # mk_auth_type - (Optional) Authentication algorithm.  Valid values are\n    mk_auth_type = \"\"\n\n    # mk_auth_key - (Optional) The auth key for the given auth type.\n    mk_auth_key = \"\"\n\n    # mk_esp_encryption_type - (Optional) The encryption algorithm.  Valid values\n    mk_esp_encryption_type = \"\"\n\n    # mk_esp_encryption_key - (Optional) The encryption key.\n    mk_esp_encryption_key = \"\"\n\n    # gps_interface - (Optional) Interface to communicate with portal.\n    gps_interface = \"\"\n\n    # gps_portal_address - (Optional) GlobalProtect portal address.\n    gps_portal_address = \"\"\n\n    # gps_publish_connected_routes - (Optional) (Optional, bool) Set to `true` to to publish\n    gps_publish_connected_routes = \"\"\n\n    # gps_publish_routes - (Optional) (Optional, list) Specify list of routes to publish\n    gps_publish_routes = \"\"\n\n    # gps_local_certificate - (Optional) GlobalProtect satellite certificate\n    gps_local_certificate = \"\"\n\n    # gps_certificate_profile - (Optional) Profile for authenticating\n    gps_certificate_profile = \"\"\n\n    # enable_tunnel_monitor - (Optional) (Optional, bool) Enable tunnel monitoring on this tunnel.\n    enable_tunnel_monitor = \"\"\n\n    # tunnel_monitor_destination_ip - (Optional) Destination IP to send ICMP probe.\n    tunnel_monitor_destination_ip = \"\"\n\n    # tunnel_monitor_source_ip - (Optional) Source IP to send ICMP probe\n    tunnel_monitor_source_ip = \"\"\n\n    # tunnel_monitor_profile - (Optional) Tunnel monitor profile.\n    tunnel_monitor_profile = \"\"\n\n    # tunnel_monitor_proxy_id - (Optional) (Optional, PAN-OS 7.0+) Which proxy-id (or\n    tunnel_monitor_proxy_id = \"\"\n\n    # Exported Attributes\n}",
        "description": "This resource allows you to add/update/delete IPSec tunnels. A large number of params have prefixes: `ak` - Auto key  `mk` - Manual key  `gps` - GlobalProtect Satellite ",
        "prefix": "panos-resource-ipsec-tunnel"
    },
    "panos-resource-ipsec-tunnel-proxy-id-ipv4": {
        "body": "resource \"panos_ipsec_tunnel_proxy_id_ipv4\" \"$1\" {\n    # ipsec_tunnel - (Required) The auto key IPSec tunnel to attach this \n    ipsec_tunnel = \"\"\n\n    # name - (Required) The object's name\n    name = \"\"\n\n    # local - (Optional) IP subnet or IP address represents local network.\n    local = \"\"\n\n    # remote - (Optional) IP subnet or IP address represents remote network.\n    remote = \"\"\n\n    # protocol_any - (Optional) (Optional, bool) Set to `true` for any IP protocol.\n    protocol_any = \"\"\n\n    # protocol_number - (Optional) (Optional, int) IP protocol number.\n    protocol_number = \"\"\n\n    # protocol_tcp_local - (Optional) (Optional, int) Local TCP port number.\n    protocol_tcp_local = \"\"\n\n    # protocol_tcp_remote - (Optional) (Optional, int) Remote TCP port number.\n    protocol_tcp_remote = \"\"\n\n    # protocol_udp_local - (Optional) (Optional, int) Local UDP port number.\n    protocol_udp_local = \"\"\n\n    # protocol_udp_remote - (Optional) (Optional, int) Remote UDP port number.\n    protocol_udp_remote = \"\"\n\n    # Exported Attributes\n}",
        "description": "This resource allows you to add/update/delete IPSec tunnel proxy IDs to\na parent auto key IPSec tunnel.",
        "prefix": "panos-resource-ipsec-tunnel-proxy-id-ipv4"
    },
    "panos-resource-license-api-key": {
        "body": "resource \"panos_license_api_key\" \"$1\" {\n    # key - (Required) The licensing API key.\n    key = \"\"\n\n    # retain_key - (Optional) Set to `true` to retain the licensing API key\n    retain_key = \"\"\n\n    # Exported Attributes\n}",
        "description": "This resource manages the licensing API key, which is necessary to delicense\nthe PAN-OS firewall. This resource's `retain_key` param is a Terraform side configuration only.  In\norder for the firewall to delicense itself, the licensing API key must be\npresent.  This means that either the `panos_licensing` resource must use\n`depends_on` and depend on this resource, or you must set the `retain_key`\nparam to `true`.  As there is no harm in leaving the licensing API key on the\nPAN-OS firewall, it is recommended that `retain_key` be set to `true`.",
        "prefix": "panos-resource-license-api-key"
    },
    "panos-resource-licensing": {
        "body": "resource \"panos_licensing\" \"$1\" {\n    # auth_codes - (Required) The list of auth codes to install.\n    auth_codes = \"\"\n\n    # delicense - (Optional) (Optional, bool) Leave as `true` if you want to delicense\n    delicense = \"\"\n\n    # mode - (Optional) For `delicense` of `true`, the type of delicensing to\n    mode = \"\"\n\n    # Exported Attributes\n}",
        "description": "This resource manages the licenses installed on the PAN-OS firewall. Installing the standard auth code for the standard PAN-OS license key for the\nfirewall causes the firewall to reboot.  Thus it is recommended that you use\nthis resource in a separate step of your overall firewall provisioning, as\nusing this resource will cause the firewall to be temporarily inaccessible.",
        "prefix": "panos-resource-licensing"
    },
    "panos-resource-loopback-interface": {
        "body": "resource \"panos_loopback_interface\" \"$1\" {\n    # name - (Required) The interface's name.  This must start with `loopback.`.\n    name = \"\"\n\n    # vsys - (Optional) The vsys that will use this interface (default: `vsys1`).\n    vsys = \"\"\n\n    # comment - (Optional) The interface comment.\n    comment = \"\"\n\n    # netflow_profile - (Optional) The netflow profile.\n    netflow_profile = \"\"\n\n    # static_ips - (Optional) List of static IPv4 addresses to set for this data\n    static_ips = \"\"\n\n    # management_profile - (Optional) The management profile.\n    management_profile = \"\"\n\n    # mtu - (Optional) The MTU.\n    mtu = \"\"\n\n    # adjust_tcp_mss - (Optional) (Optional, bool) Adjust TCP MSS (default: false).\n    adjust_tcp_mss = \"\"\n\n    # Exported Attributes\n}",
        "description": "This resource allows you to add/update/delete loopback interfaces.",
        "prefix": "panos-resource-loopback-interface"
    },
    "panos-resource-management-profile": {
        "body": "resource \"panos_management_profile\" \"$1\" {\n    # name - (Required) The management profile's name.\n    name = \"\"\n\n    # ping - (Optional) Allow ping.\n    ping = \"\"\n\n    # telnet - (Optional) Allow telnet.\n    telnet = \"\"\n\n    # ssh - (Optional) Allow SSH.\n    ssh = \"\"\n\n    # http - (Optional) Allow HTTP.\n    http = \"\"\n\n    # http_ocsp - (Optional) Allow HTTP OCSP.\n    http_ocsp = \"\"\n\n    # https - (Optional) Allow HTTPS.\n    https = \"\"\n\n    # snmp - (Optional) Allow SNMP.\n    snmp = \"\"\n\n    # response_pages - (Optional) Allow response pages.\n    response_pages = \"\"\n\n    # userid_service - (Optional) Allow User ID service.\n    userid_service = \"\"\n\n    # userid_syslog_listener_ssl - (Optional) Allow User ID syslog listener\n    userid_syslog_listener_ssl = \"\"\n\n    # userid_syslog_listener_udp - (Optional) Allow User ID syslog listener\n    userid_syslog_listener_udp = \"\"\n\n    # permitted_ips - (Optional) The list of permitted IP addresses or address\n    permitted_ips = \"\"\n\n    # Exported Attributes\n}",
        "description": "This resource allows you to add/update/delete interface management profiles.",
        "prefix": "panos-resource-management-profile"
    },
    "panos-resource-nat-rule": {
        "body": "resource \"panos_nat_rule\" \"$1\" {\n    # destination_addresses - (Required) List of destination address(es).\n    destination_addresses = \"\"\n\n    # source_addresses - (Required) List of source address(es).\n    source_addresses = \"\"\n\n    # destination_zone - (Required) The destination zone.\n    destination_zone = \"\"\n\n    # source_zones - (Required) The list of source zone(s).\n    source_zones = \"\"\n\n    # name - (Required) The NAT rule's name.\n    name = \"\"\n\n    # vsys - (Optional) The vsys to put the NAT rule into (default:\n    vsys = \"\"\n\n    # rulebase - (Optional) (Optional, Deprecated) The rulebase.  For firewalls, there is only the\n    rulebase = \"\"\n\n    # rulebase - (Optional) `rulebase` value (default), but on Panorama, there is also `pre-rulebase`\n    rulebase = \"\"\n\n    # description - (Optional) The description.\n    description = \"\"\n\n    # type - (Optional) \n    type = \"\"\n\n    # to_interface - (Optional) Egress interface from route lookup (default:\n    to_interface = \"\"\n\n    # any - (Optional) `any`).\n    any = \"\"\n\n    # service - (Optional) Service (default: `any`).\n    service = \"\"\n\n    # sat_type - (Optional) Type of source address translation.  This can be\n    sat_type = \"\"\n\n    # none - (Optional) `none` (default), `dynamic-ip-and-port`, `dynamic-ip`, or `static-ip`.\n    none = \"\"\n\n    # sat_address_type - (Optional) Source address translation address type.\n    sat_address_type = \"\"\n\n    # sat_translated_addresses - (Optional) Source address translation list of\n    sat_translated_addresses = \"\"\n\n    # sat_interface - (Optional) Source address translation interface.\n    sat_interface = \"\"\n\n    # sat_ip_address - (Optional) Source address translation IP address.\n    sat_ip_address = \"\"\n\n    # sat_fallback_type - (Optional) Source address translation fallback type.\n    sat_fallback_type = \"\"\n\n    # sat_fallback_translated_addresses - (Optional) Source address translation\n    sat_fallback_translated_addresses = \"\"\n\n    # sat_fallback_interface - (Optional) Source address translation fallback\n    sat_fallback_interface = \"\"\n\n    # sat_fallback_ip_type - (Optional) Source address translation fallback\n    sat_fallback_ip_type = \"\"\n\n    # sat_fallback_ip_address - (Optional) The source address translation\n    sat_fallback_ip_address = \"\"\n\n    # sat_static_translated_address - (Optional) The statically translated source\n    sat_static_translated_address = \"\"\n\n    # sat_static_bi_directional - (Optional) Set to `true` to enable\n    sat_static_bi_directional = \"\"\n\n    # dat_type - (Optional) Destination address translation type.  This should\n    dat_type = \"\"\n\n    # dat_address - (Optional) Destination address translation's address.  Requires\n    dat_address = \"\"\n\n    # dat_type - (Optional) `dat_type` be set to \"static\" or \"dynamic\".\n    dat_type = \"\"\n\n    # dat_port - (Optional) Destination address translation's port number.  Requires\n    dat_port = \"\"\n\n    # dat_type - (Optional) `dat_type` be set to \"static\" or \"dynamic\".\n    dat_type = \"\"\n\n    # dat_dynamic_distribution - (Optional) (Optional, PAN-OS 8.1+) Distribution algorithm\n    dat_dynamic_distribution = \"\"\n\n    # disabled - (Optional) Set to `true` to disable this rule.\n    disabled = \"\"\n\n    # tags - (Optional) List of administrative tags.\n    tags = \"\"\n\n    # Exported Attributes\n}",
        "description": "This resource allows you to add/update/delete NAT rules. ~> **Note:** `panos_nat_policy` is known as `panos_nat_rule`. The prefix `sat` stands for \"Source Address Translation\" while the prefix \"dat\"\nstands for \"Destination Address Translation\".  The order of the params in\nthis resource and their naming matches how the params are presented in\nthe GUI.  Thus, having a GUI window open while creating your resource\ndefinition will simplify the process. Note that while many of the params for this resource are optional in an\nabsolute sense, depending on what type of NAT you wish to configure, certain\nparams may become necessary to correctly configure the NAT rule.",
        "prefix": "panos-resource-nat-rule"
    },
    "panos-resource-security-policy": {
        "body": "resource \"panos_security_policy\" \"$1\" {\n    # categories - (Required) List of categories.\n    categories = \"\"\n\n    # services - (Required) List of services.\n    services = \"\"\n\n    # applications - (Required) List of applications.\n    applications = \"\"\n\n    # destination_addresses - (Required) List of destination addresses.\n    destination_addresses = \"\"\n\n    # destination_zones - (Required) List of destination zones.\n    destination_zones = \"\"\n\n    # hip_profiles - (Required) List of HIP profiles.\n    hip_profiles = \"\"\n\n    # source_users - (Required) List of source users.\n    source_users = \"\"\n\n    # source_addresses - (Required) List of source addresses.\n    source_addresses = \"\"\n\n    # source_zones - (Required) List of source zones.\n    source_zones = \"\"\n\n    # name - (Required) The security rule name.\n    name = \"\"\n\n    # vsys - (Optional) The vsys to put the security policy into (default:\n    vsys = \"\"\n\n    # rulebase - (Optional) (Optional, Deprecated) The rulebase.  For firewalls, there is only the\n    rulebase = \"\"\n\n    # rulebase - (Optional) `rulebase` value (default), but on Panorama, there is also `pre-rulebase`\n    rulebase = \"\"\n\n    # rule - (Optional) A security rule definition (see below).  The security rule\n    rule = \"\"\n\n    # type - (Optional) Rule type.  This can be `universal` (default),\n    type = \"\"\n\n    # interzone - (Optional) `interzone`, or `intrazone`.\n    interzone = \"\"\n\n    # description - (Optional) The description.\n    description = \"\"\n\n    # tags - (Optional) List of tags for this security rule.\n    tags = \"\"\n\n    # negate_source - (Optional) (Optional, bool) If the source should be negated.\n    negate_source = \"\"\n\n    # negate_destination - (Optional) (Optional, bool) If the destination should be negated.\n    negate_destination = \"\"\n\n    # action - (Optional) Action for the matched traffic.  This can be `allow`\n    action = \"\"\n\n    # log_setting - (Optional) Log forwarding profile.\n    log_setting = \"\"\n\n    # log_start - (Optional) (Optional, bool) Log the start of the traffic flow.\n    log_start = \"\"\n\n    # log_end - (Optional) (Optional, bool) Log the end of the traffic flow (default: `true`).\n    log_end = \"\"\n\n    # disabled - (Optional) (Optional, bool) Set to `true` to disable this rule.\n    disabled = \"\"\n\n    # schedule - (Optional) The security policy schedule.\n    schedule = \"\"\n\n    # icmp_unreachable - (Optional) Set to `true` to enable ICMP unreachable.\n    icmp_unreachable = \"\"\n\n    # disable_server_response_inspection - (Optional) Set to `true` to disable\n    disable_server_response_inspection = \"\"\n\n    # group - (Optional) Profile Setting: `Group` - The group profile name.\n    group = \"\"\n\n    # virus - (Optional) Profile Setting: `Profiles` - The antivirus setting.\n    virus = \"\"\n\n    # spyware - (Optional) Profile Setting: `Profiles` - The anti-spyware\n    spyware = \"\"\n\n    # vulnerability - (Optional) Profile Setting: `Profiles` - The Vulnerability\n    vulnerability = \"\"\n\n    # url_filtering - (Optional) Profile Setting: `Profiles` - The URL filtering\n    url_filtering = \"\"\n\n    # file_blocking - (Optional) Profile Setting: `Profiles` - The file blocking\n    file_blocking = \"\"\n\n    # wildfire_analysis - (Optional) Profile Setting: `Profiles` - The WildFire\n    wildfire_analysis = \"\"\n\n    # data_filtering - (Optional) Profile Setting: `Profiles` - The Data\n    data_filtering = \"\"\n\n    # Exported Attributes\n}",
        "description": "This resource allows you to manage the full security posture. ~> **Note:** `panos_security_policies` is known as `panos_security_policy`. This resource manages the full set of security rules in a vsys, enforcing both\nthe contents of individual rules as well as their ordering.  Rules are defined\nin a `rule` config block. For each security rule, there are three styles of profile settings: `None` (the default)  `Group`  `Profiles`  The Profile Setting is implicitly chosen based on what params are configured\nfor the security rule.  If you want a Profile Setting of `Group`, then the\n`group` param should be set to the desired Group Profile.  If you want a\nProfile Setting of `Profiles`, then you will need to specify one or more of\nthe following params: `virus`  `spyware`  `vulnerability`  `url_filtering`  `file_blocking`  `wildfire_analysis`  `data_filtering`  If the `group` param and none of the `Profiles` params are specified, then\nthe Profile Setting is set to `None`.",
        "prefix": "panos-resource-security-policy"
    },
    "panos-resource-security-rule-group": {
        "body": "resource \"panos_security_rule_group\" \"$1\" {\n    # categories - (Required) List of categories.\n    categories = \"\"\n\n    # services - (Required) List of services.\n    services = \"\"\n\n    # applications - (Required) List of applications.\n    applications = \"\"\n\n    # destination_addresses - (Required) List of destination addresses.\n    destination_addresses = \"\"\n\n    # destination_zones - (Required) List of destination zones.\n    destination_zones = \"\"\n\n    # hip_profiles - (Required) List of HIP profiles.\n    hip_profiles = \"\"\n\n    # source_users - (Required) List of source users.\n    source_users = \"\"\n\n    # source_addresses - (Required) List of source addresses.\n    source_addresses = \"\"\n\n    # source_zones - (Required) List of source zones.\n    source_zones = \"\"\n\n    # name - (Required) The security rule name.\n    name = \"\"\n\n    # vsys - (Optional) The vsys to put the security rule into (default:\n    vsys = \"\"\n\n    # position_keyword - (Optional) A positioning keyword for this group.  This\n    position_keyword = \"\"\n\n    # bottom - (Optional) `bottom`, or left empty (the default) to have no particular placement.  This\n    bottom = \"\"\n\n    # position_reference - (Optional) Required if `position_keyword` is one of the\n    position_reference = \"\"\n\n    # rule - (Optional) The security rule definition (see below).  The security rule\n    rule = \"\"\n\n    # type - (Optional) Rule type.  This can be `universal` (default),\n    type = \"\"\n\n    # interzone - (Optional) `interzone`, or `intrazone`.\n    interzone = \"\"\n\n    # description - (Optional) The description.\n    description = \"\"\n\n    # tags - (Optional) List of tags for this security rule.\n    tags = \"\"\n\n    # negate_source - (Optional) (Optional, bool) If the source should be negated.\n    negate_source = \"\"\n\n    # negate_destination - (Optional) (Optional, bool) If the destination should be negated.\n    negate_destination = \"\"\n\n    # action - (Optional) Action for the matched traffic.  This can be `allow`\n    action = \"\"\n\n    # log_setting - (Optional) Log forwarding profile.\n    log_setting = \"\"\n\n    # log_start - (Optional) (Optional, bool) Log the start of the traffic flow.\n    log_start = \"\"\n\n    # log_end - (Optional) (Optional, bool) Log the end of the traffic flow (default: `true`).\n    log_end = \"\"\n\n    # disabled - (Optional) (Optional, bool) Set to `true` to disable this rule.\n    disabled = \"\"\n\n    # schedule - (Optional) The security rule schedule.\n    schedule = \"\"\n\n    # icmp_unreachable - (Optional) Set to `true` to enable ICMP unreachable.\n    icmp_unreachable = \"\"\n\n    # disable_server_response_inspection - (Optional) Set to `true` to disable\n    disable_server_response_inspection = \"\"\n\n    # group - (Optional) Profile Setting: `Group` - The group profile name.\n    group = \"\"\n\n    # virus - (Optional) Profile Setting: `Profiles` - The antivirus setting.\n    virus = \"\"\n\n    # spyware - (Optional) Profile Setting: `Profiles` - The anti-spyware\n    spyware = \"\"\n\n    # vulnerability - (Optional) Profile Setting: `Profiles` - The Vulnerability\n    vulnerability = \"\"\n\n    # url_filtering - (Optional) Profile Setting: `Profiles` - The URL filtering\n    url_filtering = \"\"\n\n    # file_blocking - (Optional) Profile Setting: `Profiles` - The file blocking\n    file_blocking = \"\"\n\n    # wildfire_analysis - (Optional) Profile Setting: `Profiles` - The WildFire\n    wildfire_analysis = \"\"\n\n    # data_filtering - (Optional) Profile Setting: `Profiles` - The Data\n    data_filtering = \"\"\n\n    # Exported Attributes\n}",
        "description": "This resource allows you to add/update/delete security rule groups. ~> **Note:** `panos_security_policy_group` is known as `panos_security_rule_group`. This resource manages clusters of security rules in a single vsys,\nenforcing both the contents of individual rules as well as their\nordering.  Rules are defined in a `rule` config block. Because this resource only manages what it's told to, it will not manage\nany rules that may already exist on the firewall.  This has\nimplications on the effective security posture of your firewall, but it\nwill allow you to spread your security rules across multiple Terraform\nstate files.  If you want to verify that the security rules are only\nwhat appears in the plan file, then you should probably be using the\n[panos_security_policy](security_policy.html) resource. Although you cannot modify non-group security rules with this\nresource, the `position_keyword` and `position_reference` parameters allow you\nto reference some other security rule that already exists, using it as\na means to ensure some rough placement within the ruleset as a whole. For each security rule, there are three styles of profile settings: `None` (the default)  `Group`  `Profiles`  The Profile Setting is implicitly chosen based on what params are configured\nfor the security rule.  If you want a Profile Setting of `Group`, then the\n`group` param should be set to the desired Group Profile.  If you want a\nProfile Setting of `Profiles`, then you will need to specify one or more of\nthe following params: `virus`  `spyware`  `vulnerability`  `url_filtering`  `file_blocking`  `wildfire_analysis`  `data_filtering`  If the `group` param and none of the `Profiles` params are specified, then\nthe Profile Setting is set to `None`.",
        "prefix": "panos-resource-security-rule-group"
    },
    "panos-resource-service-group": {
        "body": "resource \"panos_service_group\" \"$1\" {\n    # services - (Required) List of services to put in this service group.\n    services = \"\"\n\n    # name - (Required) The service group's name.\n    name = \"\"\n\n    # vsys - (Optional) The vsys to put the service group into (default:\n    vsys = \"\"\n\n    # tags - (Optional) List of administrative tags.\n    tags = \"\"\n\n    # Exported Attributes\n}",
        "description": "This resource allows you to add/update/delete service groups.",
        "prefix": "panos-resource-service-group"
    },
    "panos-resource-service-object": {
        "body": "resource \"panos_service_object\" \"$1\" {\n    # destination_port - (Required) The destination port.  This can be a single\n    destination_port = \"\"\n\n    # protocol - (Required) The service's protocol.  This should be `tcp` or\n    protocol = \"\"\n\n    # name - (Required) The service object's name.\n    name = \"\"\n\n    # vsys - (Optional) The vsys to put the service object into (default:\n    vsys = \"\"\n\n    # description - (Optional) The service object's description.\n    description = \"\"\n\n    # udp - (Optional) `udp`.\n    udp = \"\"\n\n    # source_port - (Optional) The source port.  This can be a single port\n    source_port = \"\"\n\n    # tags - (Optional) List of administrative tags.\n    tags = \"\"\n\n    # Exported Attributes\n}",
        "description": "This resource allows you to add/update/delete service objects.",
        "prefix": "panos-resource-service-object"
    },
    "panos-resource-static-route-ipv4": {
        "body": "resource \"panos_static_route_ipv4\" \"$1\" {\n    # destination - (Required) Destination IP address / prefix.\n    destination = \"\"\n\n    # virtual_router - (Required) The virtual router to add the static\n    virtual_router = \"\"\n\n    # name - (Required) The address object's name.\n    name = \"\"\n\n    # interface - (Optional) Interface to use.\n    interface = \"\"\n\n    # type - (Optional) The next hop type.  Valid values are `ip-address` (the\n    type = \"\"\n\n    # next_hop - (Optional) The value for the `type` setting.\n    next_hop = \"\"\n\n    # admin_distance - (Optional) The admin distance.\n    admin_distance = \"\"\n\n    # metric - (Optional) (Optional, int) Metric value / path cost (default: `10`).\n    metric = \"\"\n\n    # route_table - (Optional) Target routing table to install the route.  Valid\n    route_table = \"\"\n\n    # bfd_profile - (Optional) (Optional, PAN-OS 7.1+) BFD configuration.\n    bfd_profile = \"\"\n\n    # Exported Attributes\n}",
        "description": "This resource allows you to add/update/delete IPv4 static routes on a\nvirtual router.",
        "prefix": "panos-resource-static-route-ipv4"
    },
    "panos-resource-telemetry": {
        "body": "resource \"panos_telemetry\" \"$1\" {\n    # application_reports - (Optional) (Bool, optional) Application reports.\n    application_reports = \"\"\n\n    # threat_prevention_reports - (Optional) (Bool, optional) Threat reports.\n    threat_prevention_reports = \"\"\n\n    # url_reports - (Optional) (Bool, optional) URL reports.\n    url_reports = \"\"\n\n    # file_type_identification_reports - (Optional) (Bool, optional) File type identification\n    file_type_identification_reports = \"\"\n\n    # threat_prevention_data - (Optional) (Bool, optional) Threat prevention data.\n    threat_prevention_data = \"\"\n\n    # threat_prevention_packet_captures - (Optional) (Bool, optional) Enable sending packet-\n    threat_prevention_packet_captures = \"\"\n\n    # threat_prevention_data - (Optional) `threat_prevention_data` also be enabled.\n    threat_prevention_data = \"\"\n\n    # product_usage_stats - (Optional) (Bool, optional) Health and performance reports.\n    product_usage_stats = \"\"\n\n    # passive_dns_monitoring - (Optional) (Bool, optional) Passive DNS monitoring.\n    passive_dns_monitoring = \"\"\n\n    # Exported Attributes\n}",
        "description": "This resource allows you to add/update/delete telemetry sharing. Join other Palo Alto Networks customers in a global sharing community, helping\nto raise the bar against the latest attack techniques. Your participation\nallows us to deliver new threat prevention controls across the attack\nlifecycle. Choose the type of data you share across applications, threat\nintelligence, and device health information to improve the fidelity of the\nprotections we deliver. This is an opt-in feature controlled with granular\npolicy, and we encourage you to join the community.",
        "prefix": "panos-resource-telemetry"
    },
    "panos-resource-tunnel-interface": {
        "body": "resource \"panos_tunnel_interface\" \"$1\" {\n    # name - (Required) The interface's name.  This must start with `tunnel.`.\n    name = \"\"\n\n    # vsys - (Optional) The vsys that will use this interface (default: `vsys1`).\n    vsys = \"\"\n\n    # comment - (Optional) The interface comment.\n    comment = \"\"\n\n    # netflow_profile - (Optional) The netflow profile.\n    netflow_profile = \"\"\n\n    # static_ips - (Optional) List of static IPv4 addresses to set for this data\n    static_ips = \"\"\n\n    # management_profile - (Optional) The management profile.\n    management_profile = \"\"\n\n    # mtu - (Optional) The MTU.\n    mtu = \"\"\n\n    # Exported Attributes\n}",
        "description": "This resource allows you to add/update/delete tunnel interfaces.",
        "prefix": "panos-resource-tunnel-interface"
    },
    "panos-resource-virtual-router": {
        "body": "resource \"panos_virtual_router\" \"$1\" {\n    # vsys - (Required) The vsys that will use this virtual router.  This should\n    vsys = \"\"\n\n    # name - (Required) The virtual router's name.\n    name = \"\"\n\n    # interfaces - (Optional) List of interfaces that should use this virtual\n    interfaces = \"\"\n\n    # static_dist - (Optional) Admin distance - Static (default: `10`).\n    static_dist = \"\"\n\n    # ospf_int_dist - (Optional) Admin distance - OSPF Int (default: `30`).\n    ospf_int_dist = \"\"\n\n    # ospf_ext_dist - (Optional) Admin distance - OSPF Ext (default: `110`).\n    ospf_ext_dist = \"\"\n\n    # ibgp_dist - (Optional) Admin distance - IBGP (default: `200`).\n    ibgp_dist = \"\"\n\n    # ebgp_dist - (Optional) Admin distance - EBGP (default: `20`).\n    ebgp_dist = \"\"\n\n    # rip_dist - (Optional) Admin distance - RIP (default: `120`).\n    rip_dist = \"\"\n\n    # Exported Attributes\n}",
        "description": "This resource allows you to add/update/delete virtual routers. **Note** - The `default` virtual router may be configured with this resource,\nhowever it will not be deleted from the firewall.  It will only be unexported\nfrom the vsys that it is currently imported in, and any interfaces imported\ninto the virtual router will be removed. This resource has some overlap with the `panos_virtual_router_entry`\nresource.  If you want to use this resource with the other one, then make\nsure that your `panos_virtual_router` spec does not define the\n`interfaces` field.",
        "prefix": "panos-resource-virtual-router"
    },
    "panos-resource-virtual-router-entry": {
        "body": "resource \"panos_virtual_router_entry\" \"$1\" {\n    # interface - (Required) The interface to import into the virtual router.\n    interface = \"\"\n\n    # virtual_router - (Required) The virtual router's name.\n    virtual_router = \"\"\n\n    # Exported Attributes\n}",
        "description": "This resource allows you to add/update/delete an interface in a\nvirtual router. This resource has some overlap with the `panos_virtual_router`\nresource.  If you want to use this resource with the other one, then make\nsure that your `panos_virtual_router` spec does not define the\n`interfaces` field.",
        "prefix": "panos-resource-virtual-router-entry"
    },
    "panos-resource-vlan-interface": {
        "body": "resource \"panos_vlan_interface\" \"$1\" {\n    # name - (Required) The interface's name.  Must start with `vlan.`.\n    name = \"\"\n\n    # vsys - (Optional) The vsys that will use this interface (default: `vsys1`).\n    vsys = \"\"\n\n    # comment - (Optional) The interface comment.\n    comment = \"\"\n\n    # netflow_profile - (Optional) The netflow profile.\n    netflow_profile = \"\"\n\n    # static_ips - (Optional) List of static IPv4 addresses to set for this data\n    static_ips = \"\"\n\n    # enable_dhcp - (Optional) Set to `true` to enable DHCP on this interface.\n    enable_dhcp = \"\"\n\n    # create_dhcp_default_route - (Optional) Set to `true` to create a DHCP\n    create_dhcp_default_route = \"\"\n\n    # dhcp_default_route_metric - (Optional) The metric for the DHCP default\n    dhcp_default_route_metric = \"\"\n\n    # management_profile - (Optional) The management profile.\n    management_profile = \"\"\n\n    # mtu - (Optional) The MTU.\n    mtu = \"\"\n\n    # adjust_tcp_mss - (Optional) Adjust TCP MSS (default: false).\n    adjust_tcp_mss = \"\"\n\n    # Exported Attributes\n}",
        "description": "This resource allows you to add/update/delete vlan interfaces.",
        "prefix": "panos-resource-vlan-interface"
    },
    "panos-resource-zone": {
        "body": "resource \"panos_zone\" \"$1\" {\n    # mode - (Required) The zone's mode.  This can be `layer3`, `layer2`,\n    mode = \"\"\n\n    # name - (Required) The zone's name.\n    name = \"\"\n\n    # vsys - (Optional) The vsys to put the zone into (default: `vsys1`).\n    vsys = \"\"\n\n    # zone_profile - (Optional) The zone protection profile.\n    zone_profile = \"\"\n\n    # log_setting - (Optional) Log setting.\n    log_setting = \"\"\n\n    # enable_user_id - (Optional) Boolean to enable user identification.\n    enable_user_id = \"\"\n\n    # interfaces - (Optional) List of interfaces to associated with this zone.\n    interfaces = \"\"\n\n    # include_acls - (Optional) Users from these addresses/subnets will\n    include_acls = \"\"\n\n    # exclude_acls - (Optional) Users from these addresses/subnets will not\n    exclude_acls = \"\"\n\n    # Exported Attributes\n}",
        "description": "This resource allows you to add/update/delete zones. This resource has some overlap with the `panos_zone_entry`\nresource.  If you want to use this resource with the other one, then make\nsure that your `panos_zone` spec does not define the\n`interfaces` field.",
        "prefix": "panos-resource-zone"
    },
    "panos-resource-zone-entry": {
        "body": "resource \"panos_zone_entry\" \"$1\" {\n    # interface - (Required) The interface's name.\n    interface = \"\"\n\n    # zone - (Required) The zone's name.\n    zone = \"\"\n\n    # vsys - (Optional) The vsys (default: `vsys1`).\n    vsys = \"\"\n\n    # mode - (Optional) The mode.  Can be `layer3` (default), `layer2`,\n    mode = \"\"\n\n    # Exported Attributes\n}",
        "description": "This resource allows you to add/update/delete a specific interface in a zone. This resource has some overlap with the `panos_zone`\nresource.  If you want to use this resource with the other one, then make\nsure that your `panos_zone` spec does not define the\n`interfaces` field. This is the appropriate resource to use if you have a pre-existing zone\nand don't want Terraform to delete it on `terraform destroy`.",
        "prefix": "panos-resource-zone-entry"
    }
}